
# Порождается приёмкой, в учёте не нужен: делается из исходника за секунду.
app/_ref-orig.html

# Зависимости сервера и его местные тайны.
server/node_modules/
server/.env
server/.env.local

# Зависимости корня (npm install) и кэш байт-кода оснастки на Python.
/node_modules/
__pycache__/
