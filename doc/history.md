# История правок

Что менялось после разбивки и почему. Устройство сборки — в `doc/build.md`,
общий обзор проекта и порядок работы — в `readme.md`.

Порядок здесь хронологический, и это не летопись ради летописи: почти каждая
запись объясняет, ПОЧЕМУ сделано так, а не иначе, и что при этом пришлось
доводить в самих приборах.

**Выводы отсюда сведены в правила** — `readme.md`, раздел «Чему научила
приёмка»: как обращаться с приборами, чем расхождение по замыслу отличается от
изъяна, почему прибор должен спрашивать о неизменном. Здесь — случаи, там —
правила; случай нужен, когда правило кажется странным и хочется понять, чем за
него заплачено.

## Визуализация метрики размером: два исправленных дефекта

Задумано так: во вкладке метрики жмёшь «Визуализировать размером» — окно
статистики закрывается, узлы меняют размер, а в легенде появляется раздел
«Активна: <метрика>» с кнопкой сброса. На деле кнопка не появлялась, зато
возникала при ПОВТОРНОМ нажатии — уже после сброса.

**Причина первая — порядок внутри функций.** `visualizeMetricBySize`
обновляла раздел легенды ДО того, как выставляла `isVisualizingBySize` и
`currentVisualizedMetric`, а `resetNodeSizes` — до их сброса. Раздел читал
прошлое состояние. Теперь обе меняют признаки, а потом вид.

**Причина вторая, найденная только на живой странице.**
`toggleMetricVisualization` сперва закрывала окно статистики, а потом читала
данные метрики из кешей — но закрытие окна возвращает метрики к живым
массивам и сбрасывает все кеши (`invalidateEverythingForScope`). Визуализация
получала пустоту и показывала «Нет данных для визуализации». Теперь данные
берутся до закрытия.

Оба дефекта давние: тот же порядок вызовов есть в исходном одностраничном
файле до всякой разбивки.

**Что это дало приборам.** Полный обход стал расходиться САМ С СОБОЙ — 34
снимка из 103 между двумя прогонами одной страницы. Визуализация теперь
по-настоящему работает и оставляет след (раздел в легенде, изменённые
радиусы), а срабатывает в разных видах от прогона к прогону: метрика
считается порциями, кнопка появляется то раньше, то позже. Обход теперь
ВОЗВРАЩАЕТ СОСТОЯНИЕ после каждого действия — снимок берётся, затем
визуализация снимается. Устойчивость восстановлена.

Путь закреплён пятью утверждениями в `assert_probe.mjs` (всего их 42).


## Приглушение: щелчок и карта сходства

Замер показал перекос. Щелчок по узлу гасит **1619 связей из 1624** (класс
`dimmed`, прозрачность 0,1) и оставляет пять смежных яркими; узлы при этом не
гаснут — работает подсветка соседей. Карта сходства («Показать на графе» в
профиле концепции) гасила **383 узла из 453**, а все 1624 связи оставались в
полную силу и перетягивали внимание ровно на то, что карта отодвигает.

Механизмы разные и намеренно: щелчок вешает КЛАССЫ и читает их в отрисовке,
карта задаёт прозрачность узлов прямо по значению сходства.

**Решение.** При включённой карте гаснут ВСЕ обычные связи (0,07). Ярким
остаётся лишь то, что карта рисует сама, — дуги к ближайшим по сходству.
Признак другой, чем при щелчке: не «несмежные», а «все обычные». Приглушать
связи ПО СХОДСТВУ нечем — у связи такого значения нет, а похожие концепции
чаще всего рёбрами и не соединены.

Оба режима закреплены утверждениями (`assert_probe.mjs`, всего их 48):
исходно все связи в полную силу; щелчок гасит несмежные и оставляет смежные;
карта гасит все; после снятия карты связи возвращаются.


## Единая всплывающая подсказка

Перепись показала ТРИ механизма на одну надобность:

| Способ | Сколько | Как выглядел |
|---|---|---|
| встроенные подсказки браузера (`title`) | 50 в разметке + 6 задавались кодом | светлые, тёмный текст; вид задаёт система, а не страница |
| свой ярлык у стрелки связи | 4 места | тёмный, но наследовал жирность и прозрачность стрелки |
| свой ярлык у рубрики и традиции | 2 места | тёмный, со своими отступами и шириной |

На стрелке всплывали ДВЕ сразу: у неё стояли и `title`, и собственный ярлык.

**Сведено к одному коробу** `#hintBox` — один на всю страницу, показывается
для любого элемента с `data-tip`; слушатели на `document`, как и всё прочее
делегирование. Тёмный фон, вес шрифта 400 при любом весе связи, перенос
длинного текста (самая длинная подсказка — 116 знаков), ширина до 320 точек.

**Почему плавающий, а не ярлык у каждого элемента.** Ярлык на CSS живёт внутри
своего элемента, и его РЕЖЕТ ПРОКРУТКА — а в стилях 30 правил с обрезкой, и
списки связей как раз прокручиваются. Короб лежит в теле страницы, поэтому не
режется; сам уходит вниз, если сверху не помещается, и не вылезает за край окна.

**Жирность ярлыка гуляла** потому, что `cw-1/2/3` задают вес шрифта и
прозрачность на САМОЙ стрелке, а ярлык лежал внутри неё и всё это наследовал.
Вес и прозрачность переехали на глиф: толщина стрелки по-прежнему кодирует вес
связи, а подсказка от этого свободна.

Замер: встроенных подсказок 0, элементов с `data-tip` — 262.
Закреплено утверждениями (`assert_probe.mjs`, всего их 59).


## Входы в окна просмотра

Собрано по коду, а не по памяти.

**Окно концепции — одиннадцать входов:** щелчок по узлу на графе; выбор в
поиске легенды и в поиске внутри окна; строка в списке связей окна концепции;
список концепций в окне философа и в форме его правки; плашка концепции в окне
связи; «Показать все концепции»; переход из профиля концепции; возврат после
сохранения правки; программный вход `openConceptById`.

**Окно философа — семь:** двойной щелчок по строке легенды; плашка философа в
окне концепции; список похожих философов; плашка в окне связи; переход из
профиля философа; возврат после сохранения правки; прямой вызов по имени.

**Окно связи — было ЧЕТЫРЕ:** щелчок по связи на графе; кнопка 🔗 в форме
правки концепции; возврат после сохранения; отмена создания новой связи. В
списках связей сама связь была видна, а открыть её было нечем — щелчок по
строке вёл к соседней КОНЦЕПЦИИ.

**Добавлен пятый: щелчок по стрелке.** Работает в окне концепции (оба списка,
внутренние и внешние), в окне философа и в описаниях пути. Стрелка сообщает о
себе: курсор-указатель, подсветка глифа при наведении, приписка в подсказке
«щёлкните, чтобы открыть связь». Возврат «← Назад» работает — стопка окон это
уже умеет.

Полный обход нашёл новый вход сам: снимков стало 406 вместо 396, обработчиков
в окне концепции 67 вместо 57.


## Три правки по замечаниям об интерфейсе

**Окно пути: исходный узел и порядок.** Блок исходного узла существовал, но
прятался ВНУТРИ первого отрезка под именем «Узел», тогда как конечный шёл
отдельным блоком «Конечный узел». Читалось как несимметричное: конец назван,
начало — нет. Теперь порядок тот же, что в самом пути:

```
Исходный узел: … → связь → Узел: … → связь → … → Конечный узел: …
```

Кнопка «Показать описания узлов» по-прежнему раскрывает все узловые блоки
разом — идентификаторы сохранены.

**Переполнение описаний — причина не в переносе слов.** Прибор показал:
`.connection-description` шире родителя ровно на сумму боковых отступов
(541 против 517), потому что при `width: 100%` они прибавляются СНАРУЖИ
ширины. Добавлены `box-sizing: border-box` и `overflow-wrap: anywhere`
(длинное слово или ссылка иначе не переносятся вовсе). После правки
переполнений ноль — мерено обходом всех элементов окна с раскрытыми
описаниями.

**Контраст: смысловой цвет выведен из-под порога.** Утверждение о контрасте
нашло стрелки пути — 2,26. Разбор показал, что дело не в стилях, а в ДАННЫХ:
цвет стрелки берётся от типа связи, и пять типов из 21 темнее порога
(`emerge_from` 1,78, `dialogue` 2,1). Поднять их значило бы переписать
смысловую палитру ради оформления, поэтому такие места пропускаются
намеренно — читаемость там решается обводкой или тенью, а не подменой цвета.

Заодно исправлены два ОСТАТКА СВЕТЛОЙ ТЕМЫ, где цвет ничего не значит:
предупреждение о хронологии было `#856404` (3,56) → `#e0b83a` (10,32);
кнопки под итогом пути — белый текст на `#3498db` (3,15) и `#e74c3c` (3,82) →
фон `#20618f` и `#a93226` (6,63 и 6,62).


## Режим хронологии «без разрывов»

**Что было не так.** Учёт хронологии проверял РЕБРО: нет ли анахронизма в
отдельной связи. Ребро может быть безупречным, а путь — ломаным. Замер по
300 парам: разрыв хронологии в 82 % найденных путей, наибольший прыжок назад
2496 лет. Пример: Эйдос (−428) → Представление (Шопенгауэр, 1788) → Мировая
воля (1788) → Стоический разум (Марк Аврелий, 121) → Логос. Каждый шаг
законен: `influence` идёт вперёд, `oppose` читается ретроспективно (Шопенгауэр
возражает стоикам), внутренние связи хронологии не имеют. Целое при этом
уходит в XIX век и возвращается во II.

**Решение — новый режим, где проверяется ПУТЬ.** Годы идут только в одну
сторону. Ход выбирается по концам: цель раньше источника — ищем ход назад, и
путь читается как родословная, «восходит к».

**Стрелка при этом не ограничивает, и это не послабление.** Связь «Платон
влияет на Аристотеля», пройденная от Аристотеля, И ЕСТЬ «восходит к»; связь
«Шопенгауэр оспаривает стоиков», пройденная от стоиков, И ЕСТЬ «его позже
оспорят». Ход времени задают ГОДЫ — они одни, и двух ограничений не нужно.
Отсюда же ответ на вопрос, читать ли ретроспективные связи как прямые:
направление чтения определяется временем, а не типом связи.

Условие оказалось МЕСТНЫМ: при монотонности крайний достигнутый год равен году
текущего узла, поэтому Дейкстра и поиск в ширину работают без переделки.

**Замер по 200 парам:**

| | найдено путей | с разрывом | средняя длина |
|---|---|---|---|
| строгий | 166 | 125 | 6 |
| **без разрывов** | **187** | **0** | 5 |

Режим не только чище, но и находит БОЛЬШЕ: строгий отбрасывает связь, если её
тип не согласуется с годами, а здесь важны только годы.

**Шапка пути показывает ход времени, а не концы.** Было «428-348 до н.э. →
121-180» — прыжок прятался между концами. Стало «428 до н.э. → 1788 → 121 ·
режим строгий · разрывов: 2». Подряд идущие повторы (шаги внутри одного
философа) сливаются, чтобы не загромождать. Разрывы считаются ПО ЧИСЛАМ, а не
по типам связей: тип говорит, как связь читается, а не куда идёт путь.


## Панель контроля переделана

Панель стояла слева внизу и перекрывалась легендой. Переделана так:

- **кнопки — только значки**, кроме трёх с подписями: «💾 JSON», «💾 PNG»,
  «💾 SVG». Вход и регистрация тоже свелись к значкам «🔑» и «📝» — иначе они
  одни держали бы панель широкой;
- **подсказка у каждой из одиннадцати кнопок** — через единый ярлык
  (`data-tip`), заводить ничего нового не пришлось;
- **кнопка справки «❓»** — знак вопроса в кружке, как у типов связей в
  легенде;
- **панель переехала вправо вниз**, на место убранной плашки подсказок.

Замер: 352×108, отступы по 20 от края, легенду НЕ перекрывает.

### Окно «О проекте»

Расширенная замена плашке в углу: та вмещала одну строку про клики, а
способов управления куда больше. Шесть разделов — что в базе, граф, легенда,
поиск пути, статистика, панель.

**Числа берутся из данных, а не вписаны руками**: 453 концепции, 1624 связи,
57 философов, годы от самого раннего до самого позднего. Числа устаревают
первыми, а править их в двух местах никто не станет.

Правка в перечень не входит намеренно — она показывается отдельным окном при
входе под учётной записью.

### Что показали приборы

Обход нашёл прибавку сам: 198 атрибутов вместо 196, 410 снимков вместо 406,
на главной 46 обработчиков вместо 44.

Два расхождения пришлось разбирать, и они РАЗНОЙ природы:

- **по замыслу** — прибор сохранности показал «было `💾 Скачать базу|📂
  Сохранить в папку`, стало `💾 JSON|📂`». Эталон переутверждён;
- **изъян прибора** — проверка заморозки требовала, чтобы при загрузке
  раскладка ЕХАЛА, а она успевала улечься: время до остановки гуляет от 1 до
  22 секунд, а прибор подключается через фиксированные три. Обе стороны
  показали одно и то же, значит не поломка. Вопрос переформулирован: не «едет
  ли она сейчас», а «есть ли ей куда ехать» — это и проверялось по смыслу.


## Четыре замечания по панели и подсказке

**Кнопки отращивали подписи.** Заморозка и группировка переписывали себе текст
при нажатии («❄️ Заморозить», «📦 Разгруппировать») — панель раздувалась на
глазах. Теперь меняется только значок, слово живёт в подсказке.

**Подсказка загораживалась панелями.** У неё стояло `z-index: 3000`, а панель
контроля при наведении поднимается до **9999**, окно правки — до **10001**:
подсказка пряталась под тем самым, к чему относилась. Поставлено 100000 — ей
место выше любого слоя: она сквозная для мыши и живёт доли секунды. Проверка
сравнивает её слой с наибольшим прочим на странице, а не с записанным числом,
поэтому не устареет при новых слоях.

**Выпадающий список режимов оставался светлым.** Его пункты рисует не
страница, а система, и без `color-scheme: dark` она рисовала их в светлой
коже. Одна строка чинит разом списки, полосы прокрутки и поля ввода; плюс
явные цвета пунктам для браузеров, которые считают иначе.

**Окно описаний пути теряло всё, кроме исходного узла.** В режиме без
разрывов путь идёт по годам и сплошь и рядом против стрелки, а
`resolvePathLinkList` искал связь только по стрелке и возвращал `null`.
Вдобавок узел выводился ВНУТРИ ветки «если связь найдена» — след моей же
прошлой правки, — так что одна ненайденная связь уносила весь дальнейший
перечень. Теперь разбор знает про режим, а узел выводится независимо.

### Прибор пришлось довести вслед за приложением

Обход разошёлся с эталоном на 34 снимках. Прежде чем что-то править,
сборка была прогнана САМА ПРОТИВ СЕБЯ — те же 34 расхождения, значит дело в
приборе. Причина: включение визуализации метрики ЗАКРЫВАЕТ окно статистики, а
возврат состояния сбрасывал только радиусы и раздел легенды. Окно оставалось
закрытым, и дальнейшие снимки ехали.

Побочная выгода: покрытие в видах статистики выросло с 22 обработчиков до 27,
снимков в первой части — со 105 до 114. Окно перестало закрываться посреди
обхода, и до части кнопок прибор раньше просто не доходил.


## Поиск: три сущности, скрытые отбором, многословный запрос

**Поиск философов в окне философа** устроен как поиск концепций: кружок цвета,
имя, под ним годы и число концепций. Разница одна: у философа нет «своего
философа», и вторая строка занята годами.

**Поиск в легенде — по трём сущностям.** Свёрнут по умолчанию, раскрывается
кнопкой; внутри три переключателя, при раскрытии выбрана «Концепция».
Переключение вида убирает найденное прежним — оно уже не про то, что
спрашивают. Связь ищется ДВУМЯ строками, как в окне связи, и второй конец
сужает выбор первого. Выбор найденной связи выделяет её на графе и ведёт к ней
взгляд.

**Философ на графе даёт ТРИ уровня, и все они в отрисовке уже были:**

| Что | Состояние | Прозрачность |
|---|---|---|
| его концепции и связи внутри него | подсвечено | 1,0 |
| связи с одним концом у него | обычное | 0,4 |
| всё прочее | приглушено | 0,1 |

Замер на Платоне: 28 подсвечено, 77 обычных, 1519 приглушено. Гасить внешние
связи наравне с посторонними значило бы терять главное — они и показывают
влияние философа за пределы его собственных понятий.

**Многословный запрос не работал вовсе** — давний изъян, есть и в нетронутом
исходнике: запрос сравнивался ЦЕЛИКОМ с началом каждого отдельного слова,
поэтому «миф о пещере» не находил ничего. Теперь каждое слово запроса ищет
себе слово, и порядок не важен.

**Скрытые отбором.** Два правила спорили молча: подсветка считала узел
участником, отрисовка его не рисовала — соседи загорались вокруг пустого
места. Теперь строка помечена словами «скрыта отбором», а выбор показывает
концепцию ПОВЕРХ отбора: в легенде появляется раздел со счётчиком и кнопкой
возврата, тем же приёмом, что сброс визуализации.

Механизм закрепления пришлось поправить: прежнее правило снимало закрепление,
едва у узла обнаруживались связи (оно писалось для новых концепций, у которых
связей ещё нет).

**Правило про латиницу.** Делегирование делает имя действия из первого вызова
в разметке, а кириллицу оно не разбирает — действие получило бы имя «onclick»
и слилось бы с любым другим таким же. Поэтому ТО, ЧТО ЗОВЁТСЯ ИЗ РАЗМЕТКИ,
НАЗЫВАЕТСЯ ЛАТИНИЦЕЙ. Записано и в коде, и в самой программе делегирования.

### Три неустойчивости приборов — один корень

Прибор спрашивал о том, что зависит от СКОРОСТИ СЧЁТА, а не от устройства:

1. ключ снимка ссылался на имя вида статистики — а обработчик срабатывает в
   том виде, где попался первым, и это гуляет (39 расхождений на ровном месте);
2. снимок хранил содержимое вида целиком — его длина скакала между 1359 и
   22 261 знаком, смотря успела ли посчитаться метрика;
3. проверка заморозки спрашивала «едет ли раскладка» и «есть ли ей куда
   ехать» — время до остановки скачет от 1 до 22 секунд.

Лечение всякий раз одно: НЕ МЕНЯТЬ состояние ради удобства прибора, а
переформулировать вопрос так, чтобы он спрашивал о неизменном. Попытка пойти
первым путём (нажимать «Рассчитать» перед снимком) роняла вкладку и была
откачена.


## Четыре состояния концепций и связи поверх отбора

**Состояний у концепции ЧЕТЫРЕ, а не три:**

| Состояние | Прозрачность | Ободок | Толщина | Свечение |
|---|---|---|---|---|
| выбранная | 1 | **жёлтый** | 6 | жёлтое |
| подсвеченная | 1 | белый | 5 | белое |
| обычная | 1 | белый | 3 | нет |
| приглушённая | **0,2** | белый | 3 | нет |

Поэтому вводить пятое для «внешних концепций выбранного философа» не нужно:
его концепции берут ПОДСВЕЧЕННОЕ состояние, соседи по внешним связям остаются
ОБЫЧНЫМИ, прочее приглушено. Жёлтый остаётся только за явным выбором.

Прежде соседи гасли до 0,2, а связи к ним оставались обычными — выходили
«связи в никуда». Замер на Платоне: 14 подсвечено, 60 обычных, 379 приглушено;
связи 28 / 77 / 1519. Уровни у концепций и у связей теперь соответствуют.

**Связи концепции, показанной поверх отбора.** Связь показывалась, только если
её пропускал отбор И оба конца видимы. Для показанной вопреки отбору первое
условие уже не действует, а связи о том не знали — концепция висела в пустоте.
Теперь связь видна, если один её конец показан поверх отбора, а другой и так
виден: у «Мифа о пещере» видны 4 связи из 8 — ровно те, что ведут к видимым
соседям.

**Строка легенды разведена по нажатиям.** Подпись была привязана к галочке
(`for`), оттого щелчок по строке СНИМАЛ философа с отбора, а двойной успевал
выключить и снова включить — мигание на ровном месте. Теперь:

| Нажатие | Что делает |
|---|---|
| галочка | включает и выключает философа в отборе |
| щелчок по строке | выбирает философа на графе |
| Ctrl+щелчок | добавляет к выбору (28 подсвеченных на двоих вместо 14) |
| двойной щелчок | окно философа |
| Shift+щелчок | правка (под учётной записью) |

Порядок разбора важен: shift проверяется первым, чтобы не спорить с выбором.

### Помехи окружения, вылеченные попутно

**Приборы ждали, пока «утихнет сеть».** С ростом дерева до ста с лишним модулей
простой не наступал вовсе: приборы падали по времени при сервере, отвечающем
200 на каждый запрос. Все девять переведены на ожидание разметки с паузой.

**Сервер приёмки стал многопоточным** (`tools/serve.py`): `python3 -m
http.server` отдаёт файлы по одному. Первая версия падала МОЛЧА — она меняла
рабочую папку, а путь получала относительный, и после смены он указывал уже не
туда. Теперь путь разворачивается до смены папки, а несуществующая папка даёт
честную ошибку вместо тишины.


## Семь замечаний по интерфейсу

**Строки выбранных философов** выделяются в легенде: жёлтая полоска слева и
лёгкий фон. Намеренно непохоже на галочку отбора — это разные вещи.

**Кнопка «Вернуть отбор»** уходит вместе со снятием выделения: показ поверх
отбора держался РАДИ выделения, без него нужды в нём нет. Первая попытка была
неверной — сброс стоял в общем сбросе подсветки, а его зовут и при НОВОЙ
подсветке, так что показ снимался в тот же миг, что и ставился.

**Раздел традиций** в окне философа по образцу рубрик в окне концепции:
название традиции, её описание и другие философы этой традиции в
хронологическом порядке.

**Значки на кнопках И БЫЛИ цветными** — в снимке кнопки 136 разных цветов.
Белыми выглядели из-за шрифта `Arial`, где нет цветных начертаний: система
подставляла запасное чёрно-белое. Семейство указано явно.

**Окно справки** не входило в общий перечень закрываемых окон — а этот перечень
и есть единственное место, куда вписывается всякое новое окно. Теперь Esc и
щелчок мимо.

**Окно описания пути**: цветная плашка философа с годами вместо строки
«Философ: имя»; заголовки концепций и связей открывают свои окна; назван вес
связи. В самой панели поиска пути узлы и стрелки тоже нажимаемы.

**Заморозка при окнах профиля РАБОТАЕТ** — замер: до окна узлы едут, при обоих
профилях стоят. Единственное наблюдение, которое не подтвердилось.

**Легенда во всю высоту.** Было 85vh: при 1080 точках внизу пропадало 80, а
содержимое легенды 3201 точка — прокрутка есть при любом размере, то есть
высота просто терялась. Стало `calc(100vh - 40px)` плюс `box-sizing` — без него
панель вылезала за край на 22 точки (та же ошибка, что была у описаний связей).

### Что поймали приборы

Карта дерева нашла круг от моих же правок: отрисовка → возврат отбора →
фильтры → отрисовка. Проверка слоёв — ребро снизу вверх: отрисовка звала
легенду. Оба одного рода, нижний слой знал о верхнем, и оба развязаны шиной:
события «философы-выбраны» и «выделение-снято», подписки ставит сборка.

Разбивка при этом оказалась сломана В СООБЩЕНИИ ОБ ОШИБКЕ: при имени без
модуля она звала несуществующую функцию, и вместо «нет модуля для такого-то»
вылетало «lineIn is not defined». Причина пряталась за собственной поломкой
инструмента — та же беда, что была с молчащим сервером.

## Раскладка по предмету: разбор 96 модулей

Раскладка считалась автоматически, с одним жёстким условием — отсутствием
кругов. Условие вычислимо, предмет — нет, и вышло **разделение по части речи**:
`labels`, `thresholds`, `toggles`, `predicates`, `misc`, `state`, `cache`,
`data`, `advanced`, `typed`. Ни одно из этих слов не отвечает на вопрос «о чём
модуль» — все отвечают на «какой части речи в нём сущности».

Проверка, по которой шёл разбор: **назначение выразимо одним предложением без
слова «разные» и без перечисления через запятую, и всякая сущность в нём под
это предложение очевидно подпадает.** Итог: годных 54, имя врёт 18,
разнородных 15, крошек 9.

**Правило переносов, найденное трижды.** Не «вверх к потребителю», а
**остаться на своём этаже, но в модуле, названном по предмету**. Подписи типов
связи звали из слоёв 4–6 сразу: перенос в `ui/legend.js` давал +6 нарушений, а
отщепление `core/relation-types.js` на нулевом этаже — ноль. Со `state.js` то
же в большем масштабе: перенос ячеек в `paths/` давал +37, а объявление
области `state/` этажом 1 — ноль.

Отсюда: **прикидка `layers.mjs если <имя> <модуль>` до переноса обязательна.**
Из полусотни задуманных переносов она отсеяла восемь, и все по этой причине.

**Чего числа не видят.** В `render/geometry.js` стояли собственные рубрики
автора (`// ---- геометрия ----`, `// ---- отрисовка сцены ----`), и модуль
пересекал границу, проведённую в тексте. Замер «тяготения» по обращениям дал
105 срабатываний, из которых настоящих 11: на общих услугах связи по всему
дому, и любой потребитель перевесит «своих». Это ровно то, о чём предупреждает
заголовок `layers.mjs` — сцепление считается по ввозам, не по обращениям.

Модулей стало 112 вместо 96. Разбор модуля с двумя предметами всегда даёт +1,
слияния убрали лишь четыре. Счёт — не цель; одно слияние (`util/html.js` в
подписи философа) было сделано и сразу откачено, потому что сливало
экранирование атрибута с подписями, то есть возвращало ту самую болезнь.

## Приборы перестали знать, где что лежит

Было 26 зашитых путей к модулям в восьми приборах. Каждый — мина: переезд
`findConnection` уронил `probe6` (ввоз не разрешился, оснастка не поднялась,
20 секунд ожидания и падение), переименование `edit-common.js` уронило
`delegate.mjs` с `ENOENT`.

Ныне прибор ввозит только `./_probe-rig.js`, а его порождает `rig.mjs` из
дерева; список имён вырос с 52 до 80. `delegate.mjs` ищет места ручной статьи
по **имени сущности**, а не по пути.

**Дважды обжёгся на списках, собранных руками.** Объединение имён для
оснастки собрал глазом и одно пропустил — `assert_probe` упал сразу. Хуже
второе: поправку `delegate.mjs` сделал через `exportsOf`, а сущности
пространств поимённо не вывозятся — три статьи из пяти молча не применились.
Сборка прошла, страница открылась, приборы строения промолчали, **а удаление
сущности перестало работать**; поймал только `probe6`. Программа при этом
честно кричала в поток ошибок, а я пропустил вывод через `tail -1`.

Отсюда в `delegate.mjs` явный останов: не нашлась хоть одна статья — сборка
прекращается. И правило: **если поиск может не найти — падай, а не
продолжай.**

Выгода видна сразу: расщепление `render/geometry.js`, откуда `assert_probe`
ввозил `linkVisualState`, прошло без единой правки в приборах.

## Вид статистики: четвёртый случай вопроса о неизменном

Снимок обхода зависел от того, **успела ли посчитаться метрика**: у `pagerank`
кнопок было 1 либо 62. После того как `boot()` укоротился вдвое, метрика стала
успевать, и снимок разошёлся с эталоном.

Прежде чем звать это дефектом, проверено три раза: два прогона сборки совпали
друг с другом, а прогон **исходника** дал ровно эталонное значение — то есть
поведение сторон не различалось ничем.

Лечение общее для всех таких случаев: не подгонять состояние под прибор, а
**спросить о неизменном**. Снимок считает теперь только то, что есть до
расчёта — заголовок, блоки пояснения, кнопку «Рассчитать»; всё, что
дорисовывает счёт, вычёркивается из клона. Первая попытка (вычёркивать одну
сетку карточек) не помогла: счёт добавляет ещё и шапку с действиями.
Переутверждение эталона здесь законно — формат снимка изменён намеренно, и
принят он по двум совпавшим полным прогонам.

## Разбор `boot`: от 1483 строк до сборки без значений

`boot.js` был вторым по величине файлом проекта и целиком лежал вне того
аппарата управления, что построен для остального: его содержимого нет в
`decisions/assign_names.json`, `remap.mjs` над ним не властен, `layers.mjs` держит его
верхним слоем по определению.

Признак, по которому сущность туда попадала: **её нельзя вычислить, пока не
случилось нечто внешнее**. Внешних вещей было четыре — разметка, `d3`, данные,
заполнение пространств. Все четыре убраны, и **ни одна правка не потребовала
трогать исходник**.

Порядок был такой:

1. **`initRefs` перестала спускаться в тела вложенных функций.** Ссылка внутри
   замыкания при инициализации не читается. Прежняя проверка уводила в `boot`
   таблицы из одних ленивых замыканий: `metricDescriptions` (456 строк),
   `FilterModes` (171), `LoadingIndicator` (57), `PROFILE_METRICS` (21 — там
   автор сам написал `() =>`, чтобы отложить чтение).
2. **Литеральное значение ячейки кладётся в модуль-владелец.** До этого
   приписка ячейки к модулю не давала файла вовсе: `state/paths.js` и соседи
   были обещанием, которого сборка не исполняла.
3. **`document` и `window` убраны из списка внешнего.** Модульный скрипт
   откладывается по стандарту и стоит перед `</body>` — разметка при
   вычислении тел уже есть. Осторожность была унаследована от одностраничной
   версии, где скрипт исполнялся посреди документа.
4. **`d3` переведён в ввоз**, **база — в статический ввоз**, **указатели
   строятся телом `graph-index`**; появился ввоз ради порядка.
5. **Разрешимость до неподвижной точки** и **`window.x = {…}` как
   объявление**.
6. **Имена пространств тоже разрешимы** при ввозе ради порядка — это увело
   домой `METRIC_COVERAGE_FN`, последнее значение.

**`boot` маскировал сцепление.** Пока `LoadingIndicator` лежал в нём (этаж 9,
откуда законно всё), нарушения слоёв не было видно; стоило вернуть таблицу в
модуль — вылезло ребро `core/feedback.js` → `filters/chains.js`: показатель
хода зовёт `CHAIN_SEARCH.cancel()`. Перекладывание по этажам лишь
переворачивало ребро; лечение нашлось по смыслу — оба про **долгую работу**,
показ хода и признак отмены, и лежат теперь вместе в `core/long-task.js`.

**Про метрики опасение не подтвердилось.** Считалось, что `METRIC_COVERAGE_FN`
не сдвинуть, пока `installMetricScopeWrappers()` подменяет метрики в таблице
на лету. Оказалось, обёртки ставятся при открытии окна статистики, то есть
много позже: таблица захватывает необёрнутые метрики в любом случае — что из
сборки, что из тела модуля. Вопрос был не в подмене, а только в порядке, и
`MET` остался изменяемым объектом.

Итог: `boot.js` 1483 → 546 строк, тело 1210 → 419, присваиваний ячейкам
92 → **0**. Признак закреплён прибором `tools/boot_values.mjs`.

## Два случая, стоящие правила

**Молчаливый пропуск страшнее громкого отказа.** Случай с `delegate.mjs` выше:
всё зелёное, а поведение испорчено.

**Смотреть надо не на итог сборки, а на то, прошла ли сборка целиком.** После
правки разборщиков в дереве обнаружились 14 рёбер снизу вверх и пять групп
кругов; я едва не начал разбирать «вскрывшееся сцепление». На деле
`prune_imports` в том прогоне упал (`eslint-scope` требует числовой версии
языка и на `'latest'` падает, тогда как `acorn` её понимает), и дерево
собралось без снятия призрачных ввозов. Полная пересборка: 0 рёбер, 0 кругов.

**У исходника два независимых читателя.** `map_globals.mjs` и `split.mjs`
разбирают его каждый сам по себе; карта не питает разбивку. Признание
`window.x = {…}` объявлением, внесённое сперва только в карту, не изменило
сборку ни на строку.

Все три вывода сведены в правила — `doc/build-ops.md`.

## Две границы, поправленные в разговоре

**Легенда и складные панели.** Было замечено, что `ui/legend.js` держит 27
имён, и предложено вынести оттуда всё «раскрытие» — секции, панели,
единообразную толщину связей. Возражение оказалось верным и опиралось на наш
же критерий: если сложить все `toggle*` в один модуль, это и будет группировка
по части речи. Доказательство рядом — `modal/descriptions.js` держит раскрытие
описаний в окнах философа, концепции и связи **по предмету**, а не по глаголу.

Проверка по реестру действий подтвердила: `toggleSection` адресован только
секциям легенды (`philosophers`, `relations`, `rubrics`, `traditions`), а
`toggleUniformLinkWidth` читает флажок из панели легенды. Оба остались.

Неуместным оказалось другое: `togglePanel` сворачивает `pathFinder`, а
`restorePanelStates` — `pathFinder` и `statsPanel`. Это панели, легендой не
являющиеся; они остались в модуле от автоматической раскладки. Отщеплён
`ui/panels.js`; в легенде 25 имён при одном предмете.

Урок: диагноз «модуль велик» может быть верен по адресу и неверен по
обоснованию. Прежде чем звать сущность чужой, надо посмотреть, **к чему она
применяется**, а не только как называется.

**`metrics/by-link-type.js`.** Прежде было записано «пять несвязанных мер,
предмета нет». Проверка кода поправила: `instrumentalIndex` считает связи типа
`instrument`, `abstractionIndex` — `exemplify`, `deductiveIndex` —
`consequence`; а `generativeIndex` смотрит все исходящие без разбора типа, и
`traditionBridgingIndex` считает по традициям философов, тип связи не трогая.

То есть имя врало не полностью, а ровно на две меры из пяти. `generativeIndex`
ушёл в `metrics/generativity.js`, межтрадиционная мостовость с кешем и двумя
порогами — в новый `metrics/tradition-bridging.js`. Оставшиеся три ключуются
на типе связи, и имя стало честным.

## Документация разделена: устройство отдельно, случаи отдельно

Разделы о ходе работы над `boot` были сперва дописаны в `readme.md` — то есть
летопись попала в документ об устройстве. Замечено, что для этого есть
отдельный файл, и правило записано в его же вступлении: «здесь — случаи, там —
правила».

Разделено так: в `readme.md` осталось то, что верно сейчас и без даты (три рода
правки, что приходит ввозом, признак `boot`, исключения в счёте слоёв), а сюда
переехали записи о случаях. Правило на будущее простое: **если запись
начинается со слова «заход» — она не в `ОБЗОР`.**

Тогда же сверен `doc/build.md`, не тронутый за всё время работы над
разбивкой: устарели команда запуска (однопоточный сервер валит приборы по
времени), описание `boot.js`, способ заполнения `DATA`, размер `S`, слой
`state.js` и мина про `METRIC_COVERAGE_FN` — она обезврежена не отложением, а
ввозом ради порядка.

## Четыре недочёта интерфейса

Замечены при работе с живой страницей; исправлены в исходнике.

**И-1. Строка философа в легенде оставалась отмеченной.** Щелчок по строке
выделяет философа на графе и красит строку классом `phil-chosen`. Класс
ставится по набору `выбранныеФилософы` и обновляется по событию
«философы-выбраны». Щелчок по фону графа звал `resetHighlight()`, а тот чистит
подсветку узлов и связей, но **не набор** и события не шлёт. Теперь щелчок по
фону означает снятие выделения целиком: набор чистится, извещение уходит.

**И-2. Окно просмотра открывалось под окном пути.** Дело в `z-index`: окно
пути 2100, окно просмотра 2000. Просмотр всегда вызывается поверх того, откуда
его позвали, — поставлено 2150 (ниже 2200 у форм правки: те поверх просмотра).
Вторая половина — общая подложка: `closePathDescriptionsModal` гасил её
безусловно, и просмотр, открытый поверх пути, оставался без затемнения и
выглядел закрытым вместе с путём. Теперь подложка гасится, только если
просмотра нет.

**И-3. Подсказка не исчезала после щелчка.** Она гаснет по `mouseleave` и по
прокрутке; щелчок обработчика не имел, и подсказка «по нему можно кликнуть»
висела над уже открытым окном. Добавлен перехват `click` рядом с `scroll`.

**И-4. Две иконки на карточке метрики перекрывались.** Обе по 32px,
абсолютные: одна `right: 12px`, вторая `right: 34px`. Первая занимает полосу
до 44px — нахлёст ровно 10px. Стало `right: 52px`, зазор 8px.

### Чему научил разбор расхождений

Правка исходника меняет поведение, и эталоны обязаны сдвинуться — но **ровно
там, где задумано**. Разбор показал не то, что ожидалось: длина каждого вида
выросла на 4020 знаков = 20 карточек × 201. Пояснение к правке, поставленное
внутри тега как `/* … */`, браузер разбирает как негодные признаки, и оно
попадает в DOM. Перенос в `<!-- … -->` перед кнопкой сделал хуже: +559 знаков
на карточку, потому что комментарий внутри шаблона карточки тоже часть
разметки. Верное место — обычный комментарий в коде, перед `return`.

После этого расхождение осталось одно и объяснимое: сдвиг позиции кнопки, длина
не изменилась. Эталоны `compare` (22 вида из 83) и `css_probe` (`z-index` и
отступ) переутверждены по двум совпавшим прогонам. Полный обход сошёлся **без**
переутверждения: 302/302 — его снимки берут облик, а не разметку.

**Правило:** переутверждать эталон только после разбора расхождений. Здесь
разбор нашёл настоящую ошибку, которую иначе заморозили бы как норму.

## Перестановка проекта: ΦilosΦaira

Корень был свалкой из шестнадцати файлов, где рядом лежали исходник, карты,
решения человека и документация. Разложено по назначению:

```
readme.md  package.json
app/         порождаемое: приложение целиком, стирается при каждой сборке
source/      единый исходный html
decisions/   решения человека: assign_names.json, layers.json
mapping/     порождаемые карты и спецификация
tools/  baseline/  doc/
```

Внутри дерева модули ушли под `modules/`, а корень дерева стал корнем
приложения: `index.html`, точка входа, сборка, стили, данные, поставляемое.
Так корень сборки годится в корень публикации.

**Раскладка хранит имена без приставки.** `modules/` — свойство дерева, а не
решение человека; переставить папку значит поправить одну функцию, а не 742
записи.

### Сперва пути, потом папки

Перестановке предшествовал вынос путей: было **44 абсолютных пути на 29
файлов**, из них девять указывали в `/home/claude/build/src`, которого давно
нет. Сведены в `tools/paths.mjs` и `tools/paths.py` — два файла, потому что
оснастка на двух языках, а общий json стал бы третьим местом, где они могут
разойтись.

Благодаря этому сам переезд стоил нескольких строк в двух файлах. Проверка
после выноса была одна и жёсткая: **сборка без единого довода обязана дать
прежнее дерево побайтово**. Она же и поймала ошибку — ввоз, вставленный
программой «после последней строки `import`», угодил внутрь шаблонов, которые
сами порождают код, и попал в `_probe-rig.js`, `main.js`, `ui/delegation.js`.

### Пять мин переезда

1. **Двойная приставка**: `../modules/core/ns.js` — путь синтаксически
   безупречный и ведущий в никуда. Поймала проверка ввозов.
2. **Три расчёта пути вместо одного** — у каждого генератора действий свой.
   Сведены в `вДереве()`, идемпотентную по приставке.
3. **Сверка по точному совпадению** в `unbridge`: `rel === 'ui/actions.js'`
   молча перестало срабатывать, и мост не снимался.
4. **Этажи исчезли, а прибор молчал.** Ни один образец `core/`, `ui/` не
   совпал с путями вида `modules/core/…`; все 112 модулей ушли в умолчание, и
   счёт нарушений показал бодрый ноль — сравнивать было нечего. Обнаружено
   случайно.
5. **Пять приборов держали имена файлов у себя**, минуя `paths`: вынос путей
   касался входов и выходов сборки, а приборы читают карты своим порядком.
   Нашлись падением с `ENOENT` сразу после переезда.

### Отчёт об объёме работы

Четвёртая мина — уже четвёртый случай, когда **проверка, у которой пропало
проверяемое, выглядит успешной** (прежние: `probe6` с зашитым путём, упавший
посреди цепочки `prune_imports`, молчаливо не применённая ручная статья).

Поэтому `layers.mjs` теперь докладывает не только приговор, но и объём:
«по этажам разложено 114, в умолчание 0». Умолчание больше двух-трёх модулей
означает, что образцы разошлись с деревом.

Первая попытка счётчика была негодной: у него оказалась **своя копия**
сопоставления, и подложенная поломка его не шелохнула. Проверка, повторяющая
логику проверяемого, не умеет поймать её отказ. Свёл этаж и счёт в одну
функцию — та же поломка теперь даёт «разложено 2, в умолчание 112».

### Решения человека собраны в одном месте

`decisions/assign_names.json` и `decisions/layers.json`. Порядок этажей до сих
пор лежал **внутри `layers.mjs`**, то есть внутри прибора: решение находилось
там, где его никто не ищет, и правка порядка выглядела правкой оснастки.

Проверено, что файл действительно читается: удаление из него одной строки
(`["ui/", 6]`) немедленно даёт «в умолчание 13» и 30 нарушений слоёв.

### Публикация

`doc/github-pages.md` переписан: содержимое `app/` уезжает в отдельную ветку
командой `git subtree push --prefix app origin gh-pages`, Pages настраивается
на неё. Адрес выходит `ranatar.github.io/philosphaira/`.

`index.html` в корне репозитория рассматривался и отклонён: сборка стирает
дерево одной строкой, и пока дерево — отдельная папка, правило «вот это
расходное» читается глазом. В корне пришлось бы перечислять дюжину имён, а
цена ошибки несимметрична — описка в перечне стирает `tools/` или `source/`.

**Приёмка после перестановки: ни один эталон не сдвинулся.** Обход 302/302,
утверждения 137/137, интерфейс 83/83, правка 28/28, сохранность 13/13,
заморозка 15/15, стили 6/6.

## Дом для оператора; переименование по разбору

`boot.js` продолжал держать десятки разнородных кусков, и я трижды подряд
объявлял его чистым — считая при этом узкую мерку («присваиваний ячейкам нет»)
и отчитываясь широким словом («осталось четыре куска»). Полный поимённый
разбор всех 65 кусков показал: **21 из них не на месте** — закрытие окна
статистики, выпадающие списки поиска, подсказки, флажки легенды, режим
хронологии, обработчики полотна.

### Как НЕ надо: правка исходника ради модулей

Первая попытка была неверной по существу: я обернул эти куски в именованные
функции **в исходнике**, чтобы разбивке было куда их положить. Приложению такие
функции не нужны — они обслуживали сборку.

Раз исходник был открыт, туда же пошли переименования кириллических имён — и
замена слова по файлу испортила базу: `дело`, `событие`, `доводы` суть обычные
русские слова, и в 137 местах описаний появились `handler`, `event`, `args`.
Поймал `probe6`: снимки `philosophers`, `concepts`, `relations` изменили длину,
чего при переименовании быть не может. Автоматическая починка сделала хуже, и
дерево пришлось восстанавливать из проверенного архива.

### Как надо: дом оператора

Оператор верхнего уровня — такая же сущность, как функция, и у него может быть
дом в раскладке. Ключ — метка плюс отпечаток текста без пробелов и
комментариев. Разбивка сама заворачивает оператор в порождённую установку
`install_<отпечаток>`, вывозит её из модуля и оставляет в сборке вызов.

Расселены **все 28 операторов разом** — дробить незачем: запись декларативна, а
приёмка одна и та же независимо от числа. `boot.js` стал 99 строк вместо 549;
`getElementById` в сборке не осталось ни одного.

Заслон подтвердил себя тут же: переименование изменило текст пяти операторов,
отпечатки поехали, и сборка **остановилась** с перечнем «в раскладке есть, в
исходнике нет». Молчаливый возврат оператора в сборку был бы худшим исходом.

### Переименование по разбору

`tools/rename_globals.mjs`: `acorn` находит узлы-имена, `eslint-scope` сводит
ссылку с объявлением, правятся только отрезки, которые разбор назвал именем.
Строка, комментарий и описание концепции недосягаемы в принципе.

26 имён, 177 мест; события шины (69 строк-ключей) заменены вместе с кавычками.
Проверка: разметка до скрипта и после совпала знак в знак, `probe6` — 28 из 28.

**Приёмка: обход 302/302, утверждения 137/137, интерфейс 83/83, правка 28/28,
сохранность 13/13, заморозка 15/15, стили 6/6. Эталоны не сдвинулись.**

### Два ребра, вскрытые расселением

Проверка архива на чистом месте показала два нарушения слоёв, которых до
расселения не было:

```
modal/core.js (5)          → boot-defs.js (9): closeAllModals
render/interactions.js (3) → ui/hint.js (6):  tooltip
```

Причина знакомая: пока операторы лежали в сборке (этаж 9), любое обращение
было законным. Перенос по смыслу сцепление проявил.

**`closeAllModals`** переносить вниз оказалось нельзя: прикидка дала +2, потому
что он зовёт `modal/entry.js`, `ui/about.js`, `modal/profile-*` — обязан знать
обо всех окнах разом, а это позволено только склейке. Значит неверным было
назначение: операторы подложки и `Escape` я отдал в `modal/core.js` по предмету
(«окна»), а по знанию им место там же, где `closeAllModals`, — в склейке.
Переназначены туда.

**`tooltip`** — `d3.select("#tooltip")`, ссылка на устаревший элемент подсказки
полотна; логики в нём нет. Лежал в `ui/hint.js` (этаж 6), нужен
`render/interactions.js` (этаж 3). Вынесен в `render/tooltip-el.js`.

**Как это было найдено — важнее самой находки.** Оба ребра нашла проверка
архива, а не я: приборы строения я гонял до расселения и после последующего
переименования, но не после самого расселения. Порядок «после каждой правки —
ВСЕ приборы строения, а не выборочно» существует ровно для этого.

### Имена установок

Порождённые установки сперва звались по отпечатку: `install_896fd83c`. Как ключ
отпечаток хорош — он неповторим и меняется при правке самого оператора, — но
как имя функции не говорит ничего ни в модуле, ни в следе ошибки в консоли
браузера.

Теперь имя даёт человек, в той же записи раскладки, а отпечаток остаётся
ключом. Все 28 установок названы по делу: `buildGroupPositions`,
`installNodeDrag`, `installHintOver`, `syncLegendWeightsToggle`,
`restoreMetricLayoutMode`.

**Неповторимость проверяется тремя проверками**, потому что имя установки
попадает в общее пространство имён: не идентификатор — отказ; совпадение с
одним из 742 имён проекта — отказ; повтор среди установок — отказ. Все три
подтверждены подложенными поломками.


### Признак ввоза `with { type: 'json' }` — поломка на публикации

База была переведена на статический ввоз, и это дало главное: `DATA` полон до
исполнения любого тела, а значит производные указатели и всё, что от них
считается, уехали из сборки по домам. Ввоз шёл прямо из `.json` с признаком
`with { type: 'json' }`.

Признак свежий. Chrome его понимает, **Firefox до 138 падает при разборе** — на
опубликованной странице не работало ничего. Приёмка поймать это не могла: она
гоняется в Chrome.

Оговорку про «ограничение по браузерам» я делал, когда вводил признак, — и всё
равно оставил его единственным способом загрузки. Оговорка в отчёте не заменяет
решения.

**Чем заменено:** ожидание на верхнем уровне модуля в `core/ns.js`
(`await Promise.all(… fetch …)`). Обещание порядка то же — всякий, кто ввозит
`ns.js`, ждёт его завершения.

**Чем НЕ заменено и почему.** Напрашивался вариант с шестью `data/*.js`
(`export default […]`) — его понимают все браузеры. Но база остаётся шестью
`.json`: их пишет сохранение из приложения, их же кладут в `data/` вручную.
Перейди чтение на `.js` — подложенный `.json` перестал бы действовать, и
молча.

В `doc/github-pages.md` дописано правило: перед публикацией открыть страницу в
Chrome и в Firefox.
