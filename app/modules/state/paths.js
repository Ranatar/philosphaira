// Сгенерировано из philosophy_graph.html — правки вносить ТУДА, не сюда.
import { S } from '../core/ns.js';
import { CHRONOLOGY_MODES } from '../core/time.js';

S.useWeightedPaths = true;

S.respectDirection = true;

S.skipTypologicalInPaths = true;

S.currentChronologyMode = CHRONOLOGY_MODES.STRICT;

S.currentPathData = null;

S.selectedSourceNode = null;

S.selectedTargetNode = null;
