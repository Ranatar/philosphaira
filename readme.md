# ΦilosΦaira

Граф философских концепций: 689 концепций, 2169 связей, 100 философов, 25
традиции. Полотно с силовой раскладкой, 39 видов статистики, поиск путей,
окна просмотра и правки.

## Раскладка проекта

| | что |
|---|---|
| `readme.md` | этот файл: устройство и порядок работы |
| `app/` | **порождаемое**: приложение целиком; стирается при каждой сборке |
| `source/` | единый исходный html — источник правды |
| `decisions/` | **решения человека**: раскладка имён и порядок этажей |
| `mapping/` | порождаемые карты и спецификация |
| `tools/` | оснастка: сборка, приборы, приёмка |
| `baseline/` | замороженные эталоны приборов |
| `doc/` | остальная документация |

Три рода правки — три документа: раскладка `doc/module-ops.md`, исходник
раздел 14 ниже, сама разбивка `doc/build-ops.md`. Третий опаснее двух первых:
он переопределяет всё дерево разом.


Один документ на весь проект: что это, из чего собрано, как менять и как
проверять. Частности — в отдельных файлах, ссылки по ходу.

---

## 1. Что это

Приложение о графе философских концепций: 689 концепций, 2169 связей,
100 философов, 25 традиций, 15 рубрик, 21 тип связи. Граф на полотне с силовой
раскладкой, 39 видов статистики с метриками, поиск путей, окна концепций,
философов и связей, режим правки под учётной записью, карта сходства.

Живёт в двух видах, и оба **действующие**:

| Вид | Что это | Зачем |
|---|---|---|
| `source/philosophy_graph_v3.html` | одностраничный файл, 31 404 строки | источник правды и **эталон приёмки** |
| `app/` | 127 модулей ES6 под `modules/`, база в семи JSON | то, что развивается дальше |

Второе **порождается** из первого. Это не две ветки, которые надо
синхронизировать: сборка воспроизводится в тот же байт из исходника и раскладки.

---

## 2. Из чего состоит сборка

Сборка — это `app/`: **127 модулей**, база в семи JSON, стили в одиннадцати
главах. Поимённый разбор — ниже, в разделе «Что каждый файл делает»: он там
один на весь документ. Прежде такой же перечень стоял и здесь, и оба устаревали
порознь — этот обещал 80 модулей.

**Пространства имён** заменили `window[имя]`, который в модулях не работает:
`DATA` — база и производные указатели, `S` — изменяемое и отложенное,
`MET` — метрики, зовомые по имени, `VIEWS` — генераторы окон.

**Разметка несёт имя действия, а не имя функции:** `data-act-click="…"` плюс
`data-a1`. Глобальных имён у приложения нет вовсе — мост снят, `bridge.js`
удалён.

---

## 3. С чего начать после распаковки

```bash
npm install                       # acorn, eslint-scope, d3 — версии ЗАКРЕПЛЕНЫ
node tools/remap.mjs проверить    # целость раскладки
node tools/remap.mjs собрать      # пересборка app/ из исходника
bash tools/serve.sh                # сервер: модули по file:// не грузятся
bash tools/accept_all.sh          # полная приёмка, обе стороны
```

**Версии закреплены точно, без `^`, и это не педантизм.** Разные выпуски
`eslint-scope` по-разному разрешают ссылки: 9.1.2 и 8.4.0 дали **разные
деревья при побайтово одинаковых входных данных** — в одном `tensionIndex`
попал в пространство метрик, в другом нет. Обнаружилось это только при
проверке архива на чистом месте.

Chrome и puppeteer берутся из окружения: переменные `CHROME` и `PUPPETEER`,
по умолчанию — пути моего окружения (раздел 6). Приборы принимают ещё `BASE`
(адрес сервера), `TREE` (папка сборки) и `KEYS` (перечень обработчиков).

## 4. Цепочка сборки

```bash
node tools/split.mjs     app/ source/philosophy_graph_v3.html decisions/assign_names.json mapping/globals_map_v3.json
node tools/delegate.mjs  app/ static     # атрибуты страницы
node tools/delegate.mjs  app/ dyn        # атрибуты генераторов
node tools/rig.mjs       app/            # оснастка приборов
node tools/unbridge.mjs  app/            # снятие моста
node tools/prune_imports.mjs app/        # призрачные ввозы
node tools/prune_exports.mjs app/        # лишние вывозы
node tools/split_css.mjs app/            # стили по главам
```

Или одной командой: `node tools/remap.mjs собрать`.

**Первый шаг стирает дерево сам.** Сборка обязана начинаться с пустого места:
остаток прошлой сборки (`actions_map.json`) однажды заставил `delegate.mjs`
счесть все действия известными и молча не подключить `actions-dyn.js` —
страница поднялась с четвертью реестра при зелёных проверках строения.
Чужую папку разбивка стирать откажется: она требует признаков дерева
(`index.html`, `main.js`, `boot.js`, `modules`).

Все программы **идемпотентны**: два прогона подряд дают одинаковое дерево. Это
не пожелание, а проверка — если дерево разошлось само с собой, значит, кто-то
правил результат вместо программы.

**Раскладка задаётся именами, а не строками** (`decisions/assign_names.json`: 764 имени
и 28 операторов верхнего уровня — у оператора тоже есть дом, см. `doc/build-ops.md`).
Номера строк сдвигаются от любой вставки: по замеру тридцать чужих сущностей
молча уезжали в соседние модули. Имя, которого в раскладке нет, **останавливает
сборку** — новая сущность требует явного решения.

---

## 5. Как что-то менять

| Что | Где | Чем |
|---|---|---|
| поведение приложения | `source/philosophy_graph_v3.html` | правка исходника, потом пересборка |
| состав модулей | `decisions/assign_names.json` | `tools/remap.mjs` |
| разметка страницы | `source/philosophy_graph_v3.html` | там же, делегирование переведёт |
| данные | `app/data/*.json` | правка через интерфейс + «Сохранить» |
| раскладка графа | `data/nodePositions.json` | НЕ РУКАМИ: `tools/layout.mjs` |
| перенос правок из дерева в исходник | `source/…v3.html` | `tools/absorb.mjs` |

**Правка в файле `app/` бессмысленна** — следующая сборка её сотрёт. Кроме
данных: их правит приложение, и вернуть их в исходник умеет `tools/absorb.mjs`
(сравнивает ЗНАЧЕНИЯ, а не текст, поэтому читаемые литералы не переписывает
ради формата).

**Наборов данных семь, и седьмой не такой, как шесть.** `nodePositions` —
раскладка, посчитанная заранее: человек её не правит никогда, она всегда
порождена из базы программой `tools/layout.mjs`. Лежит в исходнике вместе с
базой не из опрятности, а по необходимости: положи её только в сборку — стороны
разойдутся ПО ПОВЕДЕНИЮ (исходник считает раскладку, сборка читает готовую), а
`probe4`, `probe5` и `graph_probe` выбирают узлы ПО КООРДИНАТАМ и покраснеют.
Свежесть стережёт `maps_fresh.mjs` по отпечатку базы.

Подробности про перестановки модулей — `doc/module-ops.md`. Кратко:

```bash
node tools/remap.mjs где <имя>                    где живёт
node tools/remap.mjs перенести <модуль> <имя...>  перенос
node tools/remap.mjs отщепить <новый> <имя...>    новый модуль
node tools/remap.mjs слить <из> <в>               слияние
node tools/remap.mjs собрать                      пересборка
```

### Когда меняется исходник

1. `node tools/map_globals.mjs <исходник> mapping/globals_map_v3.json` — пересобрать карту
2. `node tools/remap.mjs проверить` — покажет сущности без модуля
3. вписать их в `decisions/assign_names.json`
4. `node tools/remap.mjs собрать`
5. **если правка задела состав концепций или связей** —
   `node tools/layout.mjs`, затем `node tools/remap.mjs собрать` ЕЩЁ РАЗ.
   Раскладка вычисляется по собранному дереву и пишется в исходник, поэтому
   она не может быть шагом самой цепочки: нужен полный круг «собрать →
   посчитать → собрать». Забыть нельзя молча — `maps_fresh.mjs` сверяет
   отпечаток базы с отпечатком в наборе позиций и ругается
6. приёмка (раздел 5)
7. `node tools/gen_spec2.mjs app/ mapping/module-spec.md` — спецификация

Правка описаний, типов, весов и рубрик раскладку НЕ задевает: она зависит
только от множества узлов и множества пар «источник — цель». Расстояние связи
и радиус столкновения — постоянные, сила пружины считается `d3` из степеней.

---

## 6. Приёмка

Двенадцать приборов, каждый снимает состояние **дважды** — по исходнику и по
сборке — и сравнивает хеши.

| Прибор | Что проверяет | Снимков |
|---|---|---|
| `sweep_all.mjs` | каждый обработчик разметки и каждый слушатель | 297 |
| `compare.mjs` | 39 видов статистики, окна, фильтры, пути, вход | 83 |
| `graph_probe.mjs` | наведение, клики, shift-клики под admin | 15 |
| `probe4.mjs` | перетаскивание, колесо, вывоз, печать, узкий экран | 15 |
| `probe5.mjs` | ctrl-клик, карта сходства | 15 |
| `probe6.mjs` | правка при вынесенной базе | 28 |
| `probe7.mjs` | сохранность правок | 13 |
| `probe8.mjs` | переключатель заморозки | 15 |
| `css_probe.mjs` | **вычисленные стили** каждого элемента в шести состояниях | 6 |

Плюс `check_modules.mjs` (несведённые имена, негодные ввозы, **недостижимые
модули**), `bridge_debt.py` (остатки глобального хода) и `boot_values.mjs`
(значения в сборке).

**Недостижимость — отдельный род поломки.** Файл на месте, разбор его хвалит,
страница открывается — а модуль не исполняется, потому что его никто не ввозит,
и всё, что он записывал в `MET`/`VIEWS`/реестр действий, отсутствует. Обход
ввозов от `main.js` называет такие модули поимённо; вне счёта только
`_probe-rig.js` и `dead.js`.

### Эталоны: приёмка без второй страницы

Шесть приборов из девяти имеют **замороженные эталоны** — снимки, объявленные
ожидаемыми. Тогда проверка идёт одним прогоном вместо двух и не требует
одностраничного исходника:

```bash
bash tools/accept_baseline.sh            # по эталонам, вдвое дешевле
bash tools/accept_all.sh                 # сравнение двух живых страниц
node tools/baseline.mjs состав           # что заморожено и что нет
```

**Замораживать можно лишь то, что воспроизводится.** Поэтому утверждение
эталона требует ДВУХ снимков одной стороны и отказывается, если они
разошлись; отказывается и когда в снимке есть хоть одна ошибка страницы.
Заслон не выдуман: `probe4` дал расхождение снимка «узкий экран» между двумя
прогонами исходника, а выбранный узел оказался разным — `phronesis` против
`ideal_state`. Вместе с ним не замораживаются `graph_probe` и `probe5`: все
трое считают от раскладки, которая меряет шаги по реальному времени.

**Но двух прогонов мало, если величина двумодальна.** Проверено опытом на
обходе: прогоны А и Б совпали полностью, утверждение прошло — а третий прогон
разошёлся с новым эталоном на 38 снимках и сошёлся с прежним. Два попадания в
одну моду неотличимы от воспроизводимости. Поэтому перед заморозкой — **три
прогона**, и лучше не подряд.

**Но три нужны не всегда.** Двумодальность жила в счёте метрик: величины,
считаемые с задержкой, попадают в снимок по-разному. Отсюда измеримый признак,
а не чутьё: правка задевает `similarityData`, метрики или укладку — три прогона;
правка касается разметки, стилей, прозрачности — довольно одного, а
освободившееся время честнее потратить на сличение сторон.

**Повтор не отвечает и на вопрос «вылечено ли».** Прошлый разбор объявил
двумодальность обхода снятой на основании четырёх совпавших прогонов — а звено,
которое якобы чинилось, оказалось непричастным: возврат режима значений работал
исправно, но легенда от режима не зависит вовсе. Причиной был глухой срок
ожидания вида, и нашёлся он прямым замером за минуту. Повтор говорит
«устойчиво», сличение сторон — «не врёт», а «вылечено» говорит только замер
самой причины.

**И повторный прогон отвечает не на тот вопрос.** Он говорит, устойчив ли
прибор; врёт ли он — говорит только сличение сторон. Возврат состояния,
написанный через имена (`__app.S.metricValueMode`), стабилизировал сборку и
молча не работал на исходнике, где это `let` и в `window` оно не попадает:
три совпавших прогона выглядели победой, а стороны разошлись на 43 снимках.
Отсюда: **о состоянии страницы прибор спрашивает разметку, а не имена** —
имена у сторон устроены по-разному, разметка одна. И сличение сторон делается
раньше проверки на устойчивость.

**Чего эталон не делает.** Он отвечает «стало ли иначе, чем было», а не
«правильно ли». Снятый с испорченной стороны, он закрепит поломку как норму —
таких случаев уже три: разбор собственного `onclick` в подсветке вкладок,
пустая проверка подсветки узла, пропавшая кнопка «Нормировать». И всякая
НАМЕРЕННАЯ правка рушит эталоны разом, без различения умысла и случайности.
Поэтому эталоны — только первый слой.

### Второй слой: утверждения о должном

`tools/assert_probe.mjs` — **148 утверждений**, записанных числами и условиями,
а не хешами. Ему не нужны ни исходник, ни эталон: он гоняется по одной сборке
и говорит, что ДОЛЖНО быть верно.

Проверяются: база и производные указатели (689 концепций, 2169 связей, и
указатели совпадают с базой по числу); все имена действий известны реестру,
встроенных обработчиков ноль, десять таблиц стилей; панели отрисованы по
данным; **каждая из 39 вкладок даёт непустое содержимое и ровно одну
подсветку**; окна называют то, что открыли; поиск не тащит постороннего;
без входа кнопки правки нет, под admin есть; правка доходит до базы и до узла
графа; сериализатор отдаёт шесть наборов; контраст подписей не ниже 4,5;
ошибок страницы нет.

Второй слой окупился сразу: он нашёл дефект, которого не видел ни один из
восьми прежних приборов, — список найденного в поиске легенды остался со
светлой кожи (белый прямоугольник поверх тёмной панели, контраст имени
философа 3,48). Сравнение двух страниц молчало, потому что **неверны были
обе**.

### Единый поиск концепций

Пять мест искали концепции по-своему. Сведены к одному устройству по образцу
поиска в окне просмотра связи: список выпадает ВЕСЬ по фокусу, в
хронологическом порядке философов и по алфавиту внутри философа; строка —
кружок цвета философа, название и под ним имя философа.

Отбор считает `отобратьКонцепции`, внутренности строки рисует
`внутренностиСтроки`, облик задан классом `.concept-row` один раз.

**Обработчик каждый вызывающий пишет БУКВАЛЬНО**, подставляя только доводы.
Подставишь весь вызов целиком (`onclick="${действие}"`) — делегирование
увидит один довод и не поймёт, что звать: строки списка станут мёртвыми, а
поиск пути с пустым выбором уведёт вкладку в петлю. Ровно на этом уже
спотыкался `modalActions`.

**Урок о проверке.** Первая проверка меню меряла РАЗМЕТКУ — сколько строк,
есть ли кружок, есть ли имя философа. Всё сходилось, а меню были мертвы.
Проверять надо НАЖАТИЕ: щелчок по строке в списке пути заполняет поле,
щелчок в легенде закрывает список.

### Третий слой: разбор при расхождении

`tools/snapshot.mjs` — общий снимок для всех приборов: не один хеш на всё, а
**ещё и по кускам** (32 на снимок). При расхождении прибор говорит не
«разошлось», а «разошлось в куске 17 из 32 (знаки 3200–3400 из 6400)» либо,
для снимков обхода, «поле 2 из 5: было `123:45`, стало `999:45`». Короткие
значения хранятся целиком.

Раньше каждый такой разбор требовал отдельной программы и целого захода — за
работу это случалось раз шесть.

```bash
# эталон кладётся в дерево, чтобы обе стороны шли с одного сервера
python3 -c "s=open('source/philosophy_graph_v3.html').read().replace(
  'https://cdnjs.cloudflare.com/ajax/libs/d3/7.8.5/d3.min.js','./vendor/d3.min.js');
  open('app/_ref-orig.html','w').write(s)"
cd app && python3 -m http.server 8711 &

node tools/sweep_all.mjs run _ref-orig.html o1.json 1   # части 1, 2, 3
node tools/sweep_all.mjs run index.html     m1.json 1
node tools/sweep_all.mjs merge свод_и.json - o1.json o2.json o3.json
node tools/sweep_all.mjs diff свод_и.json свод_м.json
```

Модули по `file://` не грузятся — **сервер обязателен**.

---

### Оснастка врала о `renderState` (починено 21 августа 2026)

`tools/rig.mjs` отдавал приборам `renderState` и `gfxCanvas` как `S.renderState`
и `S.gfxCanvas`. В пространствах имён их нет вовсе: оба вывозятся из
`render/canvas-core.js`. Поэтому `graph_probe`, `probe4` и `probe5` падали со
стороны сборки на `Cannot read properties of undefined (reading 'transform')` —
и падали давно, на любой копии дерева.

Обиднее всего, что лечение было описано в самой оснастке двумя строками выше,
для `selectedNodes` и `selectedEdges`: «смотрим В ОБА МЕСТА: сперва `S`, потом
вывоз. Иначе прибор врёт при каждой такой перестановке». Приём был записан и
применён наполовину.

Починено: оснастка ввозит оба имени из модуля и смотрит в оба места (имён
90 → 92), а три прибора спрашивают `A.renderState` вместо `A.S.renderState`.
После этого `probe4` 15 из 15, `probe5` 15 из 15, `graph_probe` 15 из 15 —
обе стороны, ошибок нет.

### Единая точка приёмки

`bash tools/accept.sh` — одна команда на весь предмет: строение,
утверждения, свои пробы (`tip_probe`, `draft_probe`), шесть эталонов,
сличение сторон, полный обход, ошибки страницы отдельным утверждением и
серверный слой. Ключи `--только-приложение` и `--только-сервер`.

До неё приёмок было две и они не знали друг о друге: `tools/accept_all.sh`
гонял приборы приложения, `server/accept.sh` — пробы сервера. «Прогнать
всё» означало помнить обе команды, а помнящий уходит вместе с беседой.

ПОПУТНО ВЫЯСНИЛОСЬ: `blind_probe` — седьмой эталон, замороженный 20
августа, — НЕ ЗАПУСКАЛСЯ НИ РАЗУ за фазы 0–4. Он не входил ни в один
скрипт, и тридцать докладов «приёмка зелёная» делались при неработавшем
приборе. Прогнан впервые 22 августа: 37 из 37. Сошёлся — но мог и не
сойтись, и узналось бы об этом только сейчас. Урок: прибор, который не
входит в единую точку, не существует.

### Развёртывание с нуля

`bash tools/deploy_check.sh` проходит путь целиком, НЕ КАСАЯСЬ рабочего
дерева: клон из репозитория → зависимости → сборка из исходника →
миграции на пустой базе → перенос графа → первый администратор → приёмка
сервера. Отдельно сверяется, что обратная выгрузка из свежей базы совпадает
с шестью файлами побайтово.

Заводилось это потому, что всё предыдущее работало В МОЁМ окружении: с уже
накаченными миграциями, уже перенесённым графом, уже заведённым
администратором. Это не проверка, а привычка, и она рвётся у первого, кто
возьмёт репозиторий чистым.

### Урок: сперва убедись, ЧЕМ меряешь

Приёмку однажды прогнали не тем браузером — Chromium 141 из playwright вместо
Chrome 131, которым учреждены эталоны, — и получили три ложных вывода разом:
что эталон `css_probe` непереносим, что `graph_probe`/`probe4`/`probe5` не
работают в этом окружении и что полный обход даёт 38 расхождений сторон.
После запуска тем браузером, что стоит умолчанием в `paths.mjs`, `css_probe`
сошёлся 6 из 6, а расхождения обхода исчезли.

Отсюда правило: **прежде чем объяснять расхождение свойствами устройства,
проверь, чем меряешь.** Первая строка разбора — версия браузера, а не гипотеза
о непереносимости. Пути в `paths.mjs` верны; перекрывать их переменной `CHROME`
нужно только тогда, когда умолчание действительно отсутствует, — и это стоит
проверить запуском `--version`, а не поиском по файловой системе.

## 7. Как это делалось в моём окружении

Практические частности, без которых половина работы уходит в песок.

**Chrome и puppeteer лежат не там, где ожидаешь.** Chrome 131 —
`~/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome`;
puppeteer 23.11.1 — в недрах `mermaid-cli`:
`~/.npm-global/lib/node_modules/@mermaid-js/mermaid-cli/node_modules/puppeteer`.
Запуск: `headless: 'new'`, ключи `--no-sandbox --disable-dev-shm-usage
--disable-gpu`.

**Памяти четыре гигабайта, и этого не хватает.** Вкладка с 400 снимками
разметки падает на двадцать девятом виде. Лечение: дробить прогон на части,
**каждую отдельным запуском процесса**, и сводить снимки потом (`run` → `merge`
→ `diff`). Две страницы в одном браузере тоже не живут — по одной на запуск.

**Версия разбора областей видимости влияет на итог.** Проверено на архиве:
`eslint-scope` 9.1.2 и 8.4.0 при одних и тех же входных данных дают разные
деревья. Поэтому версии в `package.json` закреплены точно.

**Разбор кода — настоящим разбором, не выражениями.** `acorn` плюс
`eslint-scope`: затенённые местные имена иначе путаются с глобальными. Карта
сущностей (`map_globals.mjs`) построена на этом и потому не врёт.

**Раскладка графа не повторяется от загрузки к загрузке.** Сила укладки считает
шаги по реальному времени; у двух прогонов **одной и той же** страницы разные
координаты — вплоть до того, что самый связный узел выпадает разный. Отсюда:
из вывезенного SVG перед сверкой вычёркиваются все числа; перетаскивание
сверяется допуском, а не точным сдвигом.

**Перед кликом по графу раскладку надо остановить.** Ждать `alpha <= 0.001`,
звать `freezeSimulation` — и **перед каждым действием заново**: закрытие окна
зовёт разморозку, и узел уезжает из-под курсора между шагами.

**Синтетическая мышь ведёт себя не как живая.** `clickCount: 2` шлёт одно
событие с `detail: 2`, а приложение считает клики само и ждёт два в пределах
300 мс. Выход за границу элемента браузер вычисляет на **следующем** входном
событии: уведёшь курсор одним прыжком — `mouseleave` придёт с запозданием на
шаг, и покажется, будто подсказка не гаснет.

**Точку на графе спрашивать у приложения, а не вычислять.** Узел может лежать
за краем окна или под панелью. Требовать: `elementFromPoint` даёт именно
полотно, `pickNode` подтверждает попадание.

---

## 8. Чему научила приёмка

Это не отступление: почти каждое правило куплено потерянным заходом.

**Прежде чем звать расхождение дефектом, прогони одну и ту же сторону дважды.**
Дважды «дефект» оказывался шумом прибора — и один раз это заметил не я, а вы.

**Прибор не должен оставлять следов в том, что измеряет.** Обход помечал
элементы своим признаком, признак попадал в хеш — 54 ложных расхождения.

**Не сравнивай длину разметки.** Она меняется от одной формы записи
обработчика: `onclick="…"` против `data-act-click="…"` плюс `data-a1`.

**Не опирайся на то, что случайно оказалось в `window`.** В обычном сценарии
туда попадают объявления функций, но **не** `let` и `const`; в сборке — только
то, что выставлено намеренно. Проверка подсветки узла годами ничего не
измеряла именно из-за этого.

**Сравнивай только снимки одной версии прибора.** Четыре раза ложная тревога
приходила оттуда, что одна сторона снята старой версией.

**Проверяй не текст правки, а результат.** Замена в программе может не
совпасть по экранированию и молча ничего не сделать — я так потерял два захода.

**Новый прибор проверяй подложенной поломкой, а не тем, что он зелёный.**
Проверка свежести карт сравнивала через `JSON.stringify` со списком ключей
вторым доводом — а там он работает отбором, и всё вложенное выпадало из
сравнения. На нарочно испорченной карте прибор говорил «свежая».

**Лечи КЛАСС, а не вход.** В беседе 4.1 запрос `/api/users/me` при запуске
давал 404 на статическом сервере, и я закрыл его меткой сервера, посчитав
вопрос решённым. В 4.5 то же самое вернулось: обход нажал кнопки панели
правок, те позвали `/api/commits`, и прибор снова насчитал ошибки страницы.
Правильный ход был очевиден с самого начала — застава в ЕДИНСТВЕННОЙ двери
наружу (`api()`), раз дверь единственная. То, что она одна, было моим же
решением, и я им не воспользовался.

**Прежде всего убедись, ЧЕМ ты меряешь.** Приёмку однажды прогнали Chromium 141
из playwright вместо Chrome 131, которым учреждены эталоны, — и получили три
ложных вывода разом: что эталон `css_probe` непереносим, что
`graph_probe`/`probe4`/`probe5` не работают в этом окружении и что полный обход
даёт 38 расхождений сторон. Верный путь при этом стоял умолчанием в
`paths.mjs`: его перекрыли переменной `CHROME` на пустом месте. Первая строка
разбора — версия браузера, а не гипотеза о непереносимости, и проверяется она
запуском `--version`, а не поиском по файловой системе.

**Имя, которое читает не человек, пишется ЛАТИНИЦЕЙ — и это шире разметки.**
Переменные оболочки не бывают кириллическими вовсе: `/bin/sh` здесь `dash`,
и `ИСХОД=...` разбирается как попытка выполнить команду — оба скрипта
приёмки провалились на первой строке. Комментарии и вывод остаются
русскими: их читает человек.

**Имя, которое зовут из разметки, пишется ЛАТИНИЦЕЙ.** `delegate.mjs`
выводит имя действия из имени функции, а кириллицу в kebab-имя не
переведёшь: обработчики получают безымянные `onclick`, `onclick-2`, и реестр
действий не сходится с ввозами — сборка встаёт. Правило исходника («все
объявления латиницей») было нарушено в беседах 4.1–4.2 и терпело ровно до
первой функции, позванной из разметки.

**Сличая стороны руками, нормализуй текст ТЕМ ЖЕ, чем нормализует прибор.**
Сырой `innerHTML` даёт 3277 знаков разницы перевода — `onclick="…"` против
`data-act-click="…"`, — и настоящие одиннадцать знаков за нею не разглядеть.
Нормализацию брать из `tools/snapshot.mjs` (`ХЕШ` снимает `on*`, `data-act-*`,
`data-aN`, `data-sweep*`).

**Переприсваиваемые величины оснастка находит САМА (с 22 августа 2026).**
`tools/rig.mjs` обходит дерево, помечает всякое имя, объявленное через
`let`, и отдаёт такие имена свойствами, а не значениями: `оснастка: имён 118
из 118; свойствами 10, значениями 102`. До этого список вёлся руками, и
правило ниже нарушалось ЧЕТЫРЕЖДЫ подряд — при том, что было записано.
Правило, которое применяется по памяти, не применяется.
УРОК ПОПУТНЫЙ: правку, которая режет и сшивает шаблон в четырёх местах, надо
делать переписыванием, а не заплатами. Три заплаты подряд выглядели верными
по одиночке и вместе не сложились — оснастка перестала складываться вовсе.

**Переприсваиваемую величину оснастка обязана отдавать СВОЙСТВОМ, а не
значением.** Положенная в `A` значением, она навсегда останется той, что была
при загрузке. Так молчал `renderState`, так же чуть не замолчал
`lastSubmitted`, и третьим попался `серверныйРежим` — прибор читал «сервера
нет» при живом сервере. Три случая подряд — уже не совпадение: у ВСЯКОГО
`let`, который отдаётся приборам, надо проверять, отдан ли он свойством.

**«Два прогона совпали» — не признак годности, когда эталон не сходится.**
Совпадение двух прогонов говорит лишь об устойчивости в этом окружении. Если
эталон при этом красный, нужен другой признак — **обратимость величины**:
вернул состояние, вернулось и значение. Именно так нашлось, что подсказки
легенды не возвращаются после «выбрать всех», а `#visualizationControlSection`
называет ту метрику, которая успела посчитаться.

**Подлог вскрывает не только слепые приборы, но и ПУСТЫЕ утверждения.**
Утверждение «новое право не достаётся администратору молча» было зелёным при
любом устройстве: списки прав вычисляются при загрузке модуля, и дописывание
имени после неё не двигает ничего. Лечение — сменить вопрос, а не подпереть
ответ: спрашивать не поведение, а текст исходника.

**Приёмка ловит не только поломки, но и неудачные решения.** Подсказку строки
легенды сперва запомнили на самом узле (`dataset.tipBase`) — `compare` тут же
показал рост снимка панели фильтров с 57 841 до 69 441 знака: шесть килобайтов
повторённого текста в разметке. Неудачное решение прожило десять минут.

**Статический сервер в этом окружении умирает сам по себе, и сторож
ОТДЕЛЬНЫМ процессом тоже не выживает.** Надёжнее оказалось проверять сервер
внутри самого прогона — перед каждой частью обхода, одной строкой `curl` и
подъёмом при неудаче. Это дешевле, чем терять получасовой прогон, и не
зависит от того, переживёт ли сторож соседний вызов.

**Статический сервер в этом окружении умирает сам по себе.** Полный обход
длится около получаса, а сервер столько не живёт: за один день он падал
трижды, и один прогон пропал целиком — причём падение выглядит как
расхождение прибора, а не как обрыв сети. Лечится сторожем: цикл, который
поднимает сервер заново, пока стоит флаг (`/tmp/serve.keep`). Без него
всякий долгий прогон — лотерея.

**Прогоняй приёмку ступенчато, а полный обход — в конце и фоном.** Строение
(`layers`, `check_modules`, `bridge_debt`, свежесть карт) — секунды, после
каждой пересборки. Дешёвые приборы (`assert_probe`, `compare`, `probe6`–`probe8`,
`css_probe`) — минуты, посреди работы. Полный обход — шесть прогонов и около
двадцати пяти минут, в один заход командной строки он не укладывается: пускать
отдельным скриптом в фоне и опрашивать. Дважды за август расхождение показывал
только он, при девяти зелёных приборах.

**Снимок снимается в НАЗВАННОМ состоянии, а не в том, какое случилось.**
Обход жмёт в том числе кнопки закрытия, и к моменту съёмки окно может
оказаться закрытым, переоткрытым или недооткрытым. За август `sweep_all`
правился по этой причине ЧЕТЫРЕЖДЫ — всякий раз в новом поле: длина вида
статистики, облик вида, отпечаток легенды, видимость подложки. Общее у всех
четырёх одно: поле не дрожало само по себе, оно честно отражало состояние,
до которого обход доходил по-разному (28 обработчиков в одном прогоне против
27 в другом). Лечение — привести состояние к названному ПЕРЕД снимком
(`await ensureStats(); await wait(300)`), а не подпирать эталон.
Из 34 точек съёмки состояние приводится явно в 10; остальные 24 полагаются
на то, что предыдущий заход оставил страницу в нужном виде. Пока они не
подводили, но это долг, а не порядок.

**Прибор о приборе полезен, если честно сказано, чего он НЕ ловит.**
`tools/snap_stability.mjs` спрашивает каждое из 23 полей снимка: дрожит ли
оно само по себе в названном состоянии (анимация, таймер, случайность).
Все 23 повторяются. Но подлог показал, что описанную выше болезнь он НЕ
видит — состояние ему называют, а обходу нет. Оговорка об этом стоит в его
шапке; без неё он однажды успокоил бы вместо того, чтобы предупредить.

**Когда стороны согласны, а эталон красный, виновата не сборка.** Это разные
вопросы: сличение сторон спрашивает «одинаково ли ведут себя исходник и
модули», эталон — «то же ли, что было раньше». Чтобы понять, ты ли это сделал,
есть третий ход: **прогнать чистую копию из git**. Так выяснилось, что 38
расхождений обхода не имеют отношения к заходу, а `probe4`/`probe5` падали
задолго до него.

### Расхождение бывает двух природ, и различать их — работа человека

Одно следует из замысла: кнопка стала называться иначе — эталон переутвердить.
Другое означает изъян. Приборы сообщают об обоих одинаково громко.

**Первый вопрос при любом расхождении — прогнать одну сторону дважды.** Если
она расходится сама с собой, виноват прибор, и в приложении искать нечего.
За последние заходы так вскрылись четыре неустойчивости подряд.

**Перестановка модулей, сломавшая снимки, — тревога. Правка разметки,
сломавшая снимки, — следствие замысла.** Во время развязки слоёв эталоны не
дрогнули ни разу: устройство менялось, поведение нет. Когда менялась разметка,
их приходилось переутверждать каждый раз, и это правильно.

### Прибор спрашивает о неизменном, а не о том, что зависит от скорости

Три неустойчивости подряд имели один корень: вопрос был про то, что гуляет от
прогона к прогону.

| Что спрашивал прибор | Почему гуляло | Как спросили заново |
|---|---|---|
| ключ снимка со ссылкой на имя вида статистики | обработчик срабатывает в том виде, где попался первым, а метрика считается порциями | ключ по области, без имени вида |
| содержимое вида статистики целиком | длина скакала между 1359 и 22 261 знаком — успела ли посчитаться метрика | облик: число кнопок, полей, заголовков, строк |
| «едет ли раскладка», «есть ли ей куда ехать» | время до остановки скачет от 1 до 22 секунд | «не заморожена ли раскладка» |

**Лечение всякий раз одно: не менять состояние ради удобства прибора, а
переформулировать вопрос.** Попытка пойти обратным путём — досчитывать метрику
перед снимком — роняла вкладку и была откачена. Это верный признак: если ради
измерения приходится добавлять тяжёлую работу, приём неверен по существу.

### Прибор обязан возвращать состояние, а не только не оставлять меток

Правило «не оставлять следов» шире, чем казалось, и всплывало трижды:

- визуализация метрики меняет радиусы и раздел легенды — обход снимает её
  после снимка;
- она же **закрывает окно статистики** — обход запоминает его состояние до
  действия и возвращает после (покрытие в видах при этом выросло с 22
  обработчиков до 27: окно перестало закрываться посреди обхода);
- один блок утверждений снимал галочку хронологии и не возвращал её —
  проверки идут по одной странице и обязаны оставлять её такой, какой взяли.

### Инструмент, который не может сказать, чего ему не хватает

Дважды причина пряталась за поломкой самого инструмента: разбивка при имени
без модуля звала несуществующую функцию и вместо «нет модуля для такого-то»
выдавала «lineIn is not defined»; сервер приёмки менял рабочую папку, получив
относительный путь, и молча отвечал «000» на любой запрос.

**Молчаливый отказ дороже громкого.** Оба лечатся одинаково: заставить
программу назвать, чего ей не хватает.

### Приборы находят больше, чем просят

Обход сам обнаруживает каждое новое нажимаемое место — за последние заходы
атрибутов стало 224 против 196, различных обработчиков 135 против 117. А
утверждения дважды нашли то, о чём никто не спрашивал: контраст стрелок пути
2,26 и белый список поиска поверх тёмной панели.

Подробности каждого случая — в `doc/history.md`; здесь только правила.

---

## 9. Стили

Таблица стилей — 117 614 знаков, 4438 строк, 654 правила — разнесена по десяти
главам: основа, легенда, общая кожа окон, окно философа, поиск пути,
хронология, статистика, итоги, напряжение, формы правки. Карту строит
`tools/css_map.mjs`, режет `tools/split_css.mjs`.

**Порядок подключения значим** — семь селекторов встречаются в двух главах, и
при равной весомости побеждает последний. Поэтому главы режутся по границам,
а не по темам вразброс: склейка глав по порядку обязана совпадать с исходным
блоком знак в знак, и разрезатель это проверяет сам.

Проверка стилей дешёвая, но нужна, и вот почему. Первый разрез сходился по
склейке знак в знак — и всё же был испорчен: якорь главы указывал в СЕРЕДИНУ
пояснения, файл открывался обрывком `*/`, браузер отбрасывал правила пачками,
198 элементов из 3780 остались без оформления. Механическая проверка такого
не ловит в принципе; поймала сверка **вычисленных** стилей. Отсюда второй
заслон: глава не может начинаться внутри пояснения.

## 10. Что осталось

ЗДЕСЬ ТОЛЬКО НЕЗАКРЫТОЕ. Записи о сделанном жили вперемешку с долгами, и
раздел на 251 строку читался как список того, что предстоит, а наполовину был
журналом. Журнал перенесён в `doc/history.md` — там же разбор каждого захода.
**Раздел, в котором сделанное и несделанное лежат рядом, перестают читать.**

- **`page_probe` этого не поймал**, хотя это ровно его предмет: он проверяет
  страницу, УЖЕ вошедшую в серверный лад, а переход из бессерверного в
  серверный у него не происходит. Прибор, не переживающий перехода, не
  стережёт переход. Отсюда правило: проверять надо СМЕНУ состояния, а не два
  состояния по отдельности.
- **Долгов переноса больше нет.** Счётчик показывает ноль по всем четырём
  строкам: атрибутов `on*` 0 из 224, имён в мосте 0, записей в `window` 0,
  обращений `window[выражение]` 0.
- **Мёртвый модуль** `dead.js` — шесть сущностей, к которым не обращается
  никто. Решение (оставить, вернуть в дело или выбросить) не принято. Полной
  изоляции у модуля нет: `metrics/cache.js` ввозит `invalidateTensionScales`,
  потому что общий сброс кешей его перечисляет.
- **Контраст глифа ЗАКРЫТ (31 августа).** Цвет берётся от ТИПА СВЯЗИ, то есть
  из данных, и переменной темы не решается — поднять его значило бы переписать
  палитру, сменив смысловое кодирование ради оформления. Сделано, как и было
  намечено: тёмный ореол под глифом. Заодно снята прозрачность (0,65 у слабых
  связей множила и без того низкий контраст, а вес и так закодирован
  жирностью — то же знание двумя способами, из которых один вредил).
  Стережёт `assert_probe` двумя утверждениями, и граница названа честно: он
  проверяет, ПРИМЕНЕНО ЛИ РЕШЕНИЕ, а не «хорош ли контраст» — формула
  контраста ореола не знает.
- **Хранилище за сервером СДЕЛАНО** (`server/`): проверка прав по правам, а не
  по ролям, разрешение столкновений по полям, история правок коммитами,
  раскладка графа в `graph_layout`. `canEdit()` удалён, заслоны спрашивают
  право. Местная сохранность (выгрузка семи JSON через File System Access)
  осталась как запасной путь: без сервера страница работает как раньше — это
  условие того, что одностраничник остаётся эталоном приёмки.
- **`core/events.js` заведён** — шина с закрытым перечнем событий и фазами;
  прямые обращения снизу вверх ею и лечатся (последний случай: створка
  раскладки на первом этаже объявляет событие вместо зова `requestDraw` с
  третьего). ОСТАЛОСЬ: `metrics/registry.js` вместо перечня из сорока
  сбрасывателей. Сейчас его роль играют прямые ввозы; это работает, но
  ацикличность держится на том, что тела модулей — почти одни объявления.
  ОПАСНОСТЬ ПЕРЕЧНЯ ЗАКРЫТА ЗАСЛОНОМ (31 августа): `resets_listed.mjs`
  проверяет, что забытых нет — сейчас их ноль из 41. Сам реестр остаётся
  долгом, но он теперь про опрятность, а не про риск неверного числа.
- **`.similar-col-hint` закрыт давно** (0,4 → 0,55, контраст 3,81 → 6,24);
  запись о нём в этом разделе устарела и снята при сверке 31 августа.
- **Развилка на будущее.** Пока обе стороны действующие, приёмка сравнивает
  сборку с исходником. Когда одностраничную версию отпустят, сравнивать станет
  не с чем — и приборы должны будут проверять не «совпадает с исходником», а
  «делает то, что должно», то есть обзавестись собственными ожиданиями. Работа
  отдельная, начинать её лучше заранее.

---

## 10-бис. Многопользовательский режим

Заведён беседами 0.1–5.2. Три вещи лежат в трёх местах, и это не случайно:

| Где | Что там | Род |
|---|---|---|
| `doc/user_management_system.md` | устройство серверной части ПО ФАКТУ построенного; в конце — чем построенное разошлось с замыслом | замысел, ставший описанием |
| `server/README.md` | порядок слоёв, приёмка, как поднять | краткое к папке |
| `server/` | сам сервер | РЕШЕНИЕ, не производное (§12) |

Читать в этом порядке: сперва `server/README.md` — чтобы поднять и
проверить; потом документ — чтобы понять, почему сделано так.

### Порядок серверной части

Снизу вверх. Ввоз идёт только вниз, и это проверяет `structure_probe`.

```
src/db/       весь SQL; rows[0] НЕ ПОКИДАЕТ этой папки
src/access/   роли и права; матрица ПОРОЖДАЕТСЯ
src/auth/     пароли, сессии, второй шаг, привратник
src/graph/    опись шести наборов, чтение и приращение
src/commits/  описание правки, слияние, применение, откат
src/users/    смена роли, бан, список
src/notify/   опись типов, складывание, доставка
src/ws/       живые соединения, шина между узлами
src/http/     маршруты, cookie, CSRF, заставы
```

### Сторона приложения

| Модуль | Что делает |
|---|---|
| `core/api.js` | единственная дверь наружу; без серверного режима закрыта |
| `core/perms.js` | набор прав как величина; сеанс толкает их сюда |
| `data/backend.js` | распорядитель: применить на месте или отправить коммитом |
| `data/remote.js` | приход извне; приращение той же дверью `afterDataChange` |
| `modal/conflict.js` | разбор столкновения: было / хотел / сейчас |
| `modal/commits.js` | панель правок: свои и очередь |
| `modal/users.js` | панель людей; допустимые роли считает СЕРВЕР |
| `ui/notifications.js` | колокол; счёт своим ходом |
| `css/11-multiuser.css` | кожа новых панелей — та же, что у прочих окон |

**Страница без сервера работает ровно как прежде.** Это не пожелание, а
проверяемое утверждение: приборы приложения гоняются офлайн на каждом
заходе, и за все фазы 0–4 ни один эталон не сдвинулся иначе как от
добавленной разметки, посчитанной поштучно.

Режим включается меткой `<meta name="philos-api">`, которую сервер
вставляет, отдавая страницу: сервер называет себя, а страница его не
нащупывает.

## 11. Слои и развязка

Порядок слоёв объявлен в `tools/layers.mjs` — это единственное, что задано
руками; всё прочее считается. Прибор показывает рёбра, идущие СНИЗУ ВВЕРХ:
их было **35 (45 имён)**, стало **0**.

```
core/ util/ (0) → data/ state.js (1) → metrics/ (2) →
render/ graph/ filters/ (3) → widgets/ paths/ (4) →
stats/ modal/ (5) → ui/ (6) → boot main (9)
```

**Три приёма развязки, в порядке предпочтения:**

1. **Перенос вниз** — если сущность оказалась общей услугой не на своём
   этаже (сообщения пользователю, поиск концепций, состояние выбора пути).
2. **Перенос по смыслу** — если она просто лежала не там: работа с графом
   уехала из слоя данных в `graph/`, выбор на полотне — туда же.
3. **Разворот зависимости через шину** (`core/events.js`) — когда перенос не
   помог бы: фильтры не обязаны знать о легенде, щелчок по полотну — об
   окнах. Список событий объявлен; известить о неизвестном — **ошибка, а не
   тишина**. Подписки ставит СБОРКА: только ей позволено знать обо всех разом.

**Отдельный слой `widgets/`** появился потому, что `ui/` смешивал две разные
вещи: сборщиков экранов (легенда) и виджеты, которыми экраны ПОЛЬЗУЮТСЯ
(выпадающий список концепций). Экран, зовущий виджет, — это вниз, а не вверх.

**Призрачные ввозы.** Разбивка ищет нужные ввозы по тексту модуля и находит
имена внутри шаблонных строк разметки. Пока обработчики были встроены, ввоз
был настоящим; после делегирования имя осталось только в строке ввоза.
Таких набралось **49 в 15 модулях** — и они давали ЛОЖНОЕ СЦЕПЛЕНИЕ: карта
считала их связями, и один из кругов держался ровно на них.
`tools/prune_imports.mjs` убирает их; шаг вписан в цепочку сборки.

**Кругов не осталось.** Было один на 43 модуля. Последние два разобраны так:

- **Цикл кадров развязан назначением.** Было замкнуто накрепко: цикл ввозил
  отрисовку, отрисовка ввозила цикл. Теперь `render/loop.js` НЕ ЗНАЕТ, что
  рисовать — рисовальщика ему назначает сборка, а если тот не назначен, цикл
  ругается вслух, а не рисует пустоту.
- **Метрики расщеплены по существу.** `metrics/scope.js` делал две разные
  вещи: отдавал чистые выборки (какие узлы и связи в области счёта, какие
  признаки включены) и распоряжался сбросом кешей. Первое уехало в
  `metrics/scope-core.js`, второе осталось.
- **Мёртвый модуль выведен из графа**: кеш напряжения и его сбрасыватель
  переехали к прочим кешам метрик, и `dead.js` перестал быть чьей-либо
  зависимостью. Ничего не выброшено, поведение не тронуто — просто склад
  больше не держит дом.

**Призрачные ввозы бывают затенёнными.** `render/geometry.js` ввозил `draw` у
отрисовщика, а внутри функции объявлял СВОЙ `const draw`: по тексту имя есть,
по делу ввоз мёртв. Уборщик, искавший по тексту, такое пропускал — переписан
на разбор областей видимости, считаются ССЫЛКИ на ввезённое имя. Так нашлись
ещё одиннадцать, все затенённые.

## 12. Раскладка и карты: решения против производного

Это различение важнее самих карт, и путать их легко.

**РЕШЕНИЯ — то, что принимает человек и что надо беречь.** Их всего два файла:

| Файл | Что в нём |
|---|---|
| `decisions/assign_names.json` | раскладка: 764 имени и 28 операторов → модуль. Перенос сущности — это правка ЗДЕСЬ, а не в дереве |
| `decisions/layers.json` | порядок этажей: ввозить можно вниз и вбок, не вверх |
| `tools/layers.mjs` (список `СЛОИ`) | объявленный порядок слоёв — единственное, что задано руками при развязке |

Потерять их — значит потерять работу: восстановить придётся по памяти. Всё
остальное восстанавливается за минуту.

**ПРОИЗВОДНОЕ — то, что пересчитывается, и потому не жалко.** Дерево `app/`
целиком, и три карты:

| Карта | По чему | Что отвечает |
|---|---|---|
| `globals_map_v3` | по одностраничному исходнику | сущности в одной области видимости, кто к кому обращается, вызовы из разметки. **Нужна разбивке** |
| `css_map` | по блоку `<style>` исходника | главы, правила, межглавные повторы селекторов |
| `map_tree` | **по собранному дереву** | кто у кого что ввозит, вывоз впустую, мёртвые сущности, круги, плотность обращений к `DATA`/`S`/`MET`/`VIEWS` |

Первые две описывают ИСТОЧНИК, третья — то, ЧТО РАБОТАЕТ. Когда одностраничную
версию отпустят, первые две исчезнут вместе с ней, третья останется.

**Что означает «все карты свежие».** Не «их не пришлось трогать», а «ни одна не
отстала от того, что описывает». Разница существенная: за одну только развязку
слоёв переехало **38 имён**, прибавилось **6** (шина и цикл кадров), модулей
стало 92 вместо 87 — и все карты при этом пересобирались по ходу, иначе сборка
просто не пошла бы. Проверку делает `tools/maps_fresh.mjs`: пересобирает каждую
заново и сличает, не считая отметки времени и записанного пути.

**Устаревают они по-разному, и это важнее, чем кажется.** `decisions/assign_names.json`
устареть незаметно НЕ МОЖЕТ: новое имя останавливает сборку с перечнем.
`globals_map_v3` устареет тише: сборка соберётся, но метрики по имени,
обработчики разметки и ячейки общего состояния окажутся посчитаны по
вчерашнему коду. А `mapping/css_map.json` не читает НИКТО (главы разрезатель берёт из
`css_map.mjs`) — эта устареет совсем молча, и это худший случай из трёх.

Поэтому есть `tools/maps_fresh.mjs`: пересобирает каждую карту во временное
место и сличает, не считая отметки времени и записанного пути. Он вписан в обе
приёмки.

**Что показала третья.** Круговая группа из 43 модулей — половина дерева
ввозит друг друга. Это не поломка (тела модулей почти одни объявления, взаимный
ввоз безопасен, приёмка зелёная), но знак, что граница между областями
проведена не по живому. Плотнее всего ввозят из `metrics/init.js`,
`stats/results.js`, `stats/modal.js`, `modal/core.js`.

**Вывоз впустую и мёртвая сущность — разные вещи.** Разбивка вывозит все
собственные имена подряд, поэтому «вывезено и никем не ввозится» встречается
Лишних вывозов теперь **ноль**: их снимает `prune_exports.mjs`, восьмой шаг
сборки. Прежде их было 203 — разбивка вывозит все собственные имена подряд, и
список вывоза переставал что-либо сообщать: обещания наружу было не отличить от
внутренней кухни.

**Что чистка не трогает.** Имя, ввезённое хоть одним модулем, `main.js` или
оснасткой `_probe-rig.js` (таких два — они и стоят в сводке отдельной мерой
«нужны приборам»). Весь `modules/dead.js`: там вывоз есть **опись склада**.
И — самое важное — **мёртвое имя вывоза не лишается**: сняв вывоз с того, что не
работает и внутри своего модуля, мы спрятали бы улику от карты, потому что
`map_tree` смотрит именно список вывоза. Проверено на себе: в первой попытке
`searchNodes` и `graphSelectionContext` исчезли из виду, и «мёртвых сущностей»
стало ноль. Чистка убирает шум, а не улики.

**Разбор прежних 203** (сделан после вопроса «неужели все они не нужны»): 194
внутри своего модуля работали и вывозились впустую — их и сняли; два нужны
приборам; две — мёртвые, оставлены на виду.

**Тот же разбор вскрыл перекос в счётчике.** `map_tree.mjs` считал обращения
обходом всего разбора, а в `export { a, b }` каждое имя даёт **два**
узла-идентификатора — `local` и `exported`. Счёт выходил завышенным ровно на
два, и всякое имя, не используемое внутри своего модуля ни разу, получало
«внутри = 2» и попадало в лишние вывозы вместо мёртвых сущностей. Вывоз
объявлением (`export function f`) перекоса не давал — потому пряталось только
перечислительное.

Показатель «мёртвых сущностей 0» держался этим перекосом. После правки их
оказалось шесть: четыре законно лежат в `modules/dead.js`, а две — находки:

| Имя | Где | Что это |
|---|---|---|
| `searchNodes` | было `core/search.js` | объявлено и не вызывается нигде, включая **исходник** |
| `graphSelectionContext` | было `state/edit.js` | тень: живое состояние — в `S`, эта копия никогда не обновлялась |

Обе **перенесены в `modules/dead.js`**: на складе шесть, мёртвых сущностей
ноль. Показатель, который никогда не ноль, не стерегут — глаз привыкает к
двойке и перестаёт отличать её от тройки.

Склад теперь считается отдельной мерой (`сложеноВСклад`): `dead.js` мёртв по
замыслу, и пока он шёл общим счётом, показатель нельзя было держать в нуле —
а значит и следить за ним. Проверено подложенным именем: неупотребимая
функция в `core/search.js` тут же попала в перечень находок. Имён, не помянутых нигде —
ни снаружи, ни внутри своего модуля, — **ноль**. Одно (`actionNames`) держится
только приборами приёмки и считается отдельно.

## 13. Что каждый файл делает

### В корне проекта

| Файл | Что это |
|---|---|
| `source/philosophy_graph_v3.html` | **исходник и эталон приёмки**: одностраничная версия, из неё собирается всё остальное |
| `app/` | сборка: 127 модулей, база в семи JSON, стили в одиннадцати главах |
| `decisions/assign_names.json` | **раскладка «имя → модуль»** — решение человека, беречь наравне с исходником |
| `mapping/globals_map_v3.json` / `.md` | карта сущностей исходника: кто к кому обращается, вызовы из разметки. Нужна разбивке |
| `mapping/css_map.json` | карта стилей: главы, правила, межглавные повторы селекторов |
| `mapping/map_tree.json` / `.md` | карта собранного дерева: состав модулей, ввозы, вывоз впустую, круги |
| `mapping/handler_keys.json` | перечень различных обработчиков разметки — по нему обход считает покрытие |
| `package.json` | зависимости с **точными** версиями (разные выпуски `eslint-scope` дают разные деревья) |
| `baseline/` | семь замороженных эталонов + `manifest.json` с датами и пояснениями |
| `readme.md` | этот файл |
| `doc/module-ops.md` | как менять состав модулей |
| `mapping/module-spec.md` | спецификация модулей, составленная из дерева |
| `doc/build.md` | устройство сборки и найденные мины |
| `doc/history.md` | что менялось после разбивки и почему — двадцать с лишним заходов; сюда же перенесён журнал из readme §10 |
| `doc/github-pages.md` | публикация на GitHub Pages |

### `tools/` — 67 программ, по назначению

**Сборка (восемь шагов, строго в этом порядке):**

| Программа | Что делает |
|---|---|
| `split.mjs` | режет исходник на модули по раскладке; **стирает дерево сам** — сборка обязана начинаться с пустого места |
| `delegate.mjs` | переводит атрибуты `on*` на делегирование; два захода: `static` и `dyn` |
| `rig.mjs` | оснастка приборов `_probe-rig.js` (не часть приложения) |
| `unbridge.mjs` | снимает мост глобальных имён |
| `prune_imports.mjs` | убирает призрачные ввозы (по разбору областей видимости) |
| `prune_exports.mjs` | снимает вывоз с имён, которых никто не ввозит; **после** чистки ввозов, иначе часть имён выглядит ввезённой призраком |
| `split_css.mjs` | режет стили на главы, кладёт `.nojekyll` |

**Управление и карты:**

| Программа | Что делает |
|---|---|
| `paths.mjs`, `paths.py` | **все пути проекта и только они**; близнецы, потому что оснастка на двух языках. Корень считается от расположения самого файла: переезд папки стоит нескольких строк здесь и нигде больше |
| `remap.mjs` | перестановки модулей и команда `собрать` — вся цепочка разом |
| `layers.mjs` | слои: `ввозы` — рёбра снизу вверх, `имя` — кто к кому ходит, `если` — прикидка переноса |
| `context.py` | окружение сущности в исходнике: ±10 соседей с пояснениями |
| `map_globals.mjs`, `map_to_md.py` | карта исходника и её читаемый вид |
| `build_invariant.mjs` | дерево воспроизводится из исходника и ничего своего не содержит |
| `content_probe.mjs` | содержание (2210 описаний, 1361 строка интерфейса…) не тронуто правкой кода |
| `rename.mjs` | переименование имён ПО ОБЛАСТЯМ ВИДИМОСТИ, с отказом при занятом имени |
| `rename_modules.mjs` | то же для связного набора модулей: правит и ввозы в соседних файлах |
| `ops_probe.mjs` | команды, переменные, пути и порты из инструкции существуют на деле |
| `formula_probe.mjs` | версии формул метрик честны: тело изменено — версия поднята |
| `plan_probe.mjs` | отметки «сделано» в плане подтверждены признаками в дереве |
| `sync_formula_versions.mjs` | версии формул на странице согласны со сводом приёмки |
| `exposure_probe.mjs` | у каждой службы есть ход наружу или записанный довод, что не надо |
| `map_tree.mjs`, `map_tree_to_md.py` | карта дерева и её читаемый вид: рёбра, круги, лишние вывозы, мёртвые сущности, склад `dead.js` и вывоз, нужный приборам, — каждое отдельной мерой |
| `css_map.mjs` | карта стилей (и перечень глав для разрезателя) |
| `gen_spec2.mjs` | спецификация модулей из дерева |
| `maps_fresh.mjs` | свежесть всех карт: пересобирает и сличает |
| `check_modules.mjs` | несведённые имена, негодные ввозы, недостижимые от `main.js` модули |
| `bridge_debt.py` | остаток долга по глобальному ходу |
| `boot_values.mjs` | значения в сборке: должно быть 0 (раздел 17) |
| `rename_globals.mjs` | переименование имён проекта по РАЗБОРУ (acorn + eslint-scope), не заменой по тексту: строки, комментарии и описания недосягаемы в принципе |
| `rename_locals.mjs` | то же для местных имён внутри модулей |
| `patch_orig_persist.py`, `patch_orig_freeze.py` | как вносились сохранность и переключатель заморозки |

**Приёмка:**

| Программа | Что проверяет |
|---|---|
| `assert_probe.mjs` | **148 утверждений о должном** — числами и условиями |
| `blind_probe.mjs` | типослепая контрольная мера против мер приложения: не читает `r.type` вовсе, и потому отвечает на вопрос «это вывод о графе или о словаре типов». Обе стороны меряет сам — числа приложения берутся из живой страницы, а не из отчёта |
| `tradition_trial.mjs` | **ОТЛОЖЕН, в приёмку не входит.** Считает последствия пяти обликов поля `traditions` (эпоха, школа, метод — порознь и вместе) по черновой разметке `decisions/tradition_kinds.draft.json`. Отложен не потому, что негоден, а потому, что вопрос преждевременен. На сентябрь 2026: 100 философов на 25 традиций дают 4,0 человека на традицию, одна традиция состоит из одного, «своих» пар 300 из 4950 — 6,1 %. Прирост базы вдвое (заходы 1–9) долю своих пар не сдвинул: знаменатель растёт быстрее числителя, и сдвинуть её может только решение о СЛОВАРЕ традиций, а не о составе росписи. Дробить шесть процентов дальше по-прежнему нечего; разбор и числа — в `doc/history.md` |
| `doc_numbers.mjs` | сверяет числа в readme с картами и деревом: число, вписанное на день написания, — отложенная поломка |
| `snap_stability.mjs` | прибор о приборе: дрожит ли каждое из 23 полей снимка само по себе. В шапке сказано, чего он НЕ ловит |
| `tip_probe.mjs` | подсказки строк легенды переживают отбор (четыре состояния) — стережёт дефект «двух хозяев у `data-tip`» |
| `draft_probe.mjs` | описание правки верно: род, действие, адрес и состав полей; нетронутое поле в описание не попадает |
| `layout_probe.mjs` | меры силовой раскладки: медиана расстояния до ближайшего соседа (90 = два радиуса столкновения), число узлов дальше 150 px, отношение «внутри традиции / между традициями», охват облака. Меряет ЖИВУЮ раскладку страницы и набор из десяти случайных стартов. Координаты не сличает: тот же `d3` в Chrome и в node даёт разные раскладки на одних данных (медиана расхождения 65 px), устойчиво только явление. Силы спрашивает у живой симуляции, а не переписывает числами |
| `resets_listed.mjs` | ни один `invalidate*Cache` из `metrics/` не забыт в общем сбросе — ни прямо, ни через посредника. Заслон к долгу о `metrics/registry.js`: опасность (завёл метрику, забыл вписать — кеш пережил смену области, и метрика показала ЧУЖОЕ ЧИСЛО молча) закрывается проверкой полноты, не дожидаясь реестра |
| `tools_listed.mjs` | перечень программ в readme сходится с папкой: все описаны, названное число верно, призраков нет. Заведён после того, как самодельная сверка трижды дала ЛОЖНУЮ тревогу — искала имя в обратных кавычках и не видела описанных прозой |
| `layout.mjs` | считает раскладку медленным отжигом (3000 тиков, распад 0.005) и вписывает её в исходник литералом `nodePositions` вместе с отпечатком базы. Считает В CHROME, а не в node: тот же `d3` в двух движках даёт разные раскладки на одних данных. Отпечаток СПРАШИВАЕТ у страницы, а не пересчитывает по своему образцу |
| `absorb.mjs` | обратный перенос: `app/data/*.json` → литералы исходника. Сравнивает значения, а не текст. Список наборов читает из `split.mjs`. Проверяется имеющимся заслоном — пересборка обязана совпасть с `app/` побайтово |
| `reorder_chrono.mjs` | **однократная перестановка, в приёмку не входит.** Расставила записи по хронологии: философов по году рождения (устойчиво — ровесники сохранили прежний относительный порядок), концепции группами по философам с сохранением порядка внутри группы, связи по месту источника и цели. Ни одно поле не изменено, набор записей сверен до знака. Отпечаток базы НЕ меняется — `graphFingerprint()` сортирует перед свёрткой, — поэтому раскладку пересчитывать не нужно. А вот замороженные эталоны переучредить пришлось: `compare`, `probe6`, `css_probe`, `blind_probe`, `sweep_all` и отпечаток содержания порядок запоминают |
| `add_relation_ids.mjs` | **однократный перенос, в приёмку не входит.** Выдал имена связям (`sha1` от тройки «источник — цель — тип»). Повторим и идемпотентен: второй прогон даёт те же имена, третий не делает ничего |
| `sweep_all.mjs` | каждый обработчик разметки и каждый слушатель (три части) |
| `compare.mjs` | 39 видов статистики, окна, фильтры, пути, вход |
| `graph_probe.mjs` | наведение, клики, shift-клики на полотне |
| `probe4.mjs` | перетаскивание, колесо, вывоз, печать, узкий экран |
| `probe5.mjs` | ctrl-клик, карта сходства |
| `probe6.mjs` | правка при вынесенной базе |
| `probe7.mjs` | сохранность правок |
| `probe8.mjs` | переключатель заморозки |
| `css_probe.mjs` | вычисленные стили каждого элемента |
| `baseline.mjs` | утверждение и сверка эталонов |
| `snapshot.mjs` | общий снимок с накопительными хешами (разбор расхождений) |
| `make_ref.py` | кладёт одностраничный исходник в дерево как `_ref-orig.html` — вторая сторона приёмки |
| `serve.py` | многопоточный сервер: модули по `file://` не грузятся, а `python3 -m http.server` отдаёт по одному файлу и приборы падают по времени |
| `serve.sh` | обёртка над `serve.py`: гасит прежний, поднимает новый, проверяет ответ |
| `accept_all.sh`, `accept_baseline.sh` | вся приёмка одной командой |

### Корень `app/`

| Файл или папка | Что это |
|---|---|
| `index.html` | разметка без скрипта; подключает `main.js` и десять таблиц стилей |
| `main.js` | ввозит все модули поимённо, ставит делегирование, запускает |
| `boot.js` | весь код, исполнявшийся на верхнем уровне, в исходном порядке; здесь же подписки шины |
| `_probe-rig.js` | точки входа для приборов, **не часть приложения** |
| `actions_map.json` | имя действия → исходный текст обработчика; без него приёмку вести нельзя |
| `namespaces.json` | какое имя в каком пространстве живёт: `DATA` 16, `S` 63, `MET` 33, `VIEWS` 6. **Единственный источник состава `S`** — счёт по тексту врёт |
| `modules/` | модули дерева (счёт ведёт `mapping/map_tree.json`, сверяет `tools/doc_numbers.mjs`); в том числе `boot-defs.js` (склейка: закрывает все окна разом, знать всех ей положено), `dead.js` (склад мёртвых сущностей) и `state/` — изменяемое состояние, разнесённое по нескольким файлам |
| `css/` | десять глав стилей; **порядок подключения значим** |
| `data/` | шесть наборов базы + чтение и сохранность |
| `core/ util/ metrics/ render/ graph/ filters/ widgets/ paths/ stats/ modal/ ui/` | модули по слоям (см. раздел 11) |
| `vendor/` | d3 7.8.5 из сети переехал к себе |

## 13-бис. Редакционная модель: происхождение и замеры

Заведено этапами E и F (`doc/plan-next.md`, `doc/gaps-2026-08-27.md`) и
касается СОДЕРЖАНИЯ, а не устройства.

**ПРОИСХОЖДЕНИЕ УТВЕРЖДЕНИЙ.** У концепций, связей и философов есть поле
`provenance` (строка) и `provenanceStatus` — одно из четырёх состояний:

| состояние | что значит |
|---|---|
| `unspecified` | источник ещё не искали; это УМОЛЧАНИЕ, поле не заводится вовсе |
| `sourced` | есть библиографическая ссылка, она в `provenance` |
| `editorial_reasoning` | собственное построение составителя, а не ссылка |
| `source_not_found` | искали и пока не нашли |

Последнее говорит о ХОДЕ РАБОТЫ, а не о предмете: завтра источник может
найтись. Путать эти вещи — значит превращать рабочую пометку в утверждение
о философии, которого никто не делал.

Сервер стережёт СОГЛАСИЕ состояния со строкой (`assertProvenance`):
«источник есть» без ссылки и «не указано» при заполненной ссылке
отвергаются. Окно правки чинит расхождение само, чтобы человек не получал
отказ за то, что окно позволило.

Заслон `provenanceDriftWarning` спрашивает при правке описания с прежним
источником: подтверждает ли он и НОВУЮ формулировку. Ссылка, оставшаяся под
переписанным текстом, удостоверяет уже не то — и выглядит убедительнее, чем
её отсутствие. В панели правок изменение основания видно рецензенту:
было → стало, состояние словами, и менялся ли текст вместе с ним.

Показатель покрытия РАЗДЕЛЬНЫЙ: «источник N · основание N · не найден N ·
не разобрано N». Прежнее «С источником: X/Y» мерило заполненность, а не
подкреплённость. **Процента достоверности нет и не будет:** достоверность
не считается делением.

**ЗАМЕРЫ МЕТРИК.** Метрики считаются в браузере и нигде не хранились —
число нельзя было процитировать. Теперь человек может ЗАПОМНИТЬ ЗАМЕР
(полоса под видом статистики), и он сохраняется вместе с условиями: версия
графа, версия формулы, флаги счёта, охват. Раздел «Замеры» показывает
запомненное и сличает два замера — но ТОЛЬКО если совпало всё, кроме
версии графа; иначе прямой отказ с указанием, что именно разошлось.
Ложная история убедительнее отсутствующей, потому её не показывают вовсе.

Версии формул живут в `baseline/formulas.json`; `tools/formula_probe.mjs`
ломает приёмку, если формула изменена, а версия нет.

**ФАЗЫ ШИНЫ.** Рассылка `data-changed` идёт по объявленным фазам:
`derived` → `bound` → `filters` → `ui`. Порядок здесь семантика: отбор
спрашивает у конца связи свойство, а концы становятся объектами только в
`bound`. Подписчик называет фазу, а не полагается на место в списке; без
фазы — в `ui`, самую позднюю.

## 14. Что запускать при каждом изменении

Порядок не произволен: каждый шаг опирается на итог предыдущего.

```bash
# 1. Правка вносится В ИСХОДНИК source/philosophy_graph_v3.html — не в app/.
#    Правка в дереве бессмысленна: следующая сборка её сотрёт.

# 2. Карта исходника — первым делом: по ней считается раскладка.
npm run check:app    # дерево воспроизводится из исходника (инвариант)
npm run build:app    # то же, что node tools/remap.mjs собрать

node tools/map_globals.mjs source/philosophy_graph_v3.html mapping/globals_map_v3.json
node tools/css_map.mjs source/philosophy_graph_v3.html mapping/css_map.json     # если трогали стили

# 3. Новые имена требуют ЯВНОГО решения — сборка иначе встанет с перечнем.
node tools/remap.mjs проверить
#    вписать новые имена в decisions/assign_names.json, затем:
node tools/remap.mjs собрать        # восемь шагов + проверка имён

# 4. Устройство: слои и круги.
node tools/layers.mjs ввозы         # должно быть 0
node tools/map_tree.mjs app mapping/map_tree.json && python3 tools/map_tree_to_md.py mapping/map_tree.json mapping/map_tree.md
python3 tools/bridge_debt.py app    # долг должен быть 0
node tools/boot_values.mjs app      # значений в сборке должно быть 0

# 5. ПРИЁМКА — ОДНОЙ КОМАНДОЙ. Она сама поднимает сервер, кладёт эталон,
#    гонит приборы приложения, обход и серверные пробы.
bash tools/accept.sh                        # приложение и сервер
bash tools/accept.sh --только-приложение    # если сервер не трогали
bash tools/accept.sh --только-сервер

#    Прежние две команды остались и годятся для быстрой проверки, но
#    единая точка полнее: в неё входят tip_probe, draft_probe, blind_probe
#    и сверка чисел в документации, которых в старых скриптах не было.
bash tools/accept_baseline.sh               # по эталонам, один прогон
bash tools/accept_all.sh                    # сравнение двух живых страниц

#    Развёртывание с нуля — отдельно, из клона, не касаясь рабочего дерева.
bash tools/deploy_check.sh

# 6. Если снимки разошлись — РЕШИТЬ, что это.
#    Перестановка модулей сломала снимки → ТРЕВОГА: устройство меняли, а
#    поведение поехало. Правка разметки сломала → ожидаемо, переутвердить:
node tools/baseline.mjs утвердить <прибор> <снимок1> <снимок2>   # ДВА прогона ОДНОЙ стороны

# 7. Карты и документы.
node tools/gen_spec2.mjs app mapping/module-spec.md
python3 tools/map_to_md.py mapping/globals_map_v3.json mapping/globals_map_v3.md
node tools/maps_fresh.mjs           # все карты свежие?

# 8. Воспроизводимость и упаковка.
#    Пересобрать в чистое место и сличить: цепочка обязана давать тот же байт.
```

**Три правила, за которые заплачено заходами.** Прежде чем звать расхождение
дефектом — прогнать ОДНУ И ТУ ЖЕ сторону дважды. Новый прибор проверять
ПОДЛОЖЕННОЙ ПОЛОМКОЙ, а не тем, что он зелёный. Проверять не текст правки, а
результат — перечитать изменённое место.

---

## 15. Три рода правки — три документа

| что правим | документ | что переопределяет |
|---|---|---|
| раскладку (какое имя в каком модуле) | `doc/module-ops.md` | размещение 764 имён |
| исходник `source/philosophy_graph_v3.html` | этот файл, раздел 14 | поведение приложения |
| саму разбивку `tools/*.mjs` | `doc/build-ops.md` | **всё дерево разом** |

Третий опаснее двух первых и потому описан отдельно.

---

## 16. Что приходит ввозом, а что — событием

Устройство запуска держится на одном различии: **ввоз даёт обещание порядка,
всё прочее — нет.** Ни `DATA`, ни `S`, ни глобальная переменная не говорят
ничего о том, вычислено ли уже то, к чему обращаешься.

Поэтому ввозом приходит всё, что может:

- **база** — `core/ns.js` читает шесть `.json` **ожиданием на верхнем уровне
  модуля** (`await Promise.all(… fetch …)`) и собирает `DATA` до исполнения
  любого другого тела: всякий, кто ввозит `ns.js`, ждёт его завершения.
  `loadData()` осталась именем и возвращает готовый `DATA`; асинхронности
  в запуске нет.
  **Не `import … with { type: 'json' }`**, хотя раздел прежде описывал именно
  это: признак свежий, и Firefox до 138 падает на нём ПРИ РАЗБОРЕ — не
  работает вообще ничего. Проверено на опубликованной странице, отмена
  записана в `doc/history.md`. Не `.js`-модули с данными: база остаётся
  шестью `.json` — их пишет сохранение из приложения, их же кладут в `data/`
  вручную; заменишь способ чтения — подложенный `.json` перестанет
  действовать молча;
- **`d3`** — `vendor/d3.js` есть обёртка над поставляемой UMD-сборкой
  (`import './d3.min.js'; export default globalThis.d3;`), и разбивка
  добавляет `import d3` в каждый модуль, где имя упомянуто; классического
  `<script src>` в разметке нет;
- **производные указатели** — `buildIndexes()` вызывается в конце тела
  `core/graph-index.js`. Модулю, читающему `DATA.<указатель>` при вычислении
  своего тела, приписывается `import './core/graph-index.js';` **без имён** —
  ввоз ради порядка;
- **имена пространств `MET` и `VIEWS`** — заполняются телами своих модулей,
  и ссылающийся на них получает такой же ввоз ради порядка.

**Что этим НЕ затронуто:** правка в приложении никогда не перечитывала базу —
она меняет `DATA` в памяти и зовёт `afterDataChange`. Обновление вида без
перезагрузки работает как прежде. Пропала лишь возможность перечитать `.json`
с диска в ходе работы, а ею никто не пользовался.

**Оговорки:** по `file://` чтение не работает, но модули там и так не
грузятся.

---

## 17. Признак `boot`: сборка соединяет, а не вычисляет

**В теле `boot()` не должно оставаться ни значений, ни дел.** Значение уезжает
в модуль-владелец; дело — тоже, потому что у оператора верхнего уровня есть
дом ровно так же, как у имени (`decisions/assign_names.json`, раздел
`операторы`; ключ и порядок — в `doc/build-ops.md`).

В сборке остаётся только связывание: `await loadData()`, вызовы установок,
подписки на шину, `onReady`/`onLoad` и несколько записей о ходе запуска. Ныне
это 100 строк против 1483 в начале разбора.

Значение уезжает домой, если всё, на что оно ссылается, **готово к моменту
вычисления тела**: встроенное, окружение (разметка есть — скрипт модульный),
`d3`, сырая база, производные указатели, обычные сущности чужих модулей, имена
пространств — и **другие разрешимые ячейки**, если поставить ввоз к их модулю.
Последнее считается до неподвижной точки: `spacingX = viewWidth / (cols + 1)`
разрешимо ровно постольку, поскольку разрешимо `viewWidth`.

**Признак проверяется прибором, а не глазом:**

```
node tools/boot_values.mjs
  boot(): кусков 69, строк 100
  значений в сборке: 0 — признак соблюдён
```

Прибор считает присваивания самим ячейкам и отличает их от обращений к
готовому. Место в приёмке — с прочими приборами строения, до запуска страницы.

**Чего он НЕ ловит и что стоит помнить.** Он видит значения, но не видит дел:
оператор, знающий об одном предмете (`getElementById('chronologyModeSelect')`),
для него неотличим от связывания. Такие места ищутся глазами по составу
`boot.js` — и, найдясь, получают дом в разделе `операторы`.


## 18. Исключения в счёте слоёв

`layers.mjs` не считает нарушением ввоз из:

- **`data/*.json`** — у файла данных нет своих ввозов, он ни от чего не
  зависит и лежит ниже всех, где бы ни лежала его папка;
- **`vendor/*`** — обёртка над поставляемой сборкой, своих зависимостей не
  имеет;
- **`dead.js`** — не часть устройства, а склад.

`check_modules` по той же причине проверяет `vendor/*` и `*.json` на наличие
файла, а не на вывозы: разбирать там нечего.
