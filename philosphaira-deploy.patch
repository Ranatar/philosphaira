diff --git a/deploy/philos-ubuntu.sh b/deploy/philos-ubuntu.sh
index 6acbe5e..4807f27 100644
--- a/deploy/philos-ubuntu.sh
+++ b/deploy/philos-ubuntu.sh
@@ -1,11 +1,12 @@
 #!/usr/bin/env bash
 # ΦilosΦaira на Ubuntu 24.04 — установка, развёртывание, управление.
 #
-# Спутник инструкции `philosphaira-развёртывание.md`: то же самое, но одной
+# Спутник инструкции `philosphaira-ubuntu.md`: то же самое, но одной
 # командой. Инструкцию стоит прочесть — здесь объяснены только те решения,
 # которые иначе выглядят произволом.
 #
 #   bash philos-ubuntu.sh установить          # пакеты, Node 22, PostgreSQL, Chrome
+#   PHILOS_NO_CHROME=1 bash philos-ubuntu.sh установить   # боевая машина: без Chrome
 #   bash philos-ubuntu.sh развернуть <путь>   # архив или каталог с проектом
 #   bash philos-ubuntu.sh пуск | стоп | состояние | журнал | адрес
 #   bash philos-ubuntu.sh служба              # автозапуск через systemd (ДВЕ службы)
@@ -127,23 +128,30 @@ mkdir -p "$LOGD"
   поставить postgresql python3 python3-venv curl unzip git
   годно "$(psql --version)"
 
-  шаг "библиотеки для Chrome"
-  # Без них Chrome на безголовой Ubuntu не стартует и молчит невнятно.
-  # `libasound2t64` — имя именно для 24.04; старое `libasound2` не найдётся.
-  поставить libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 \
-    libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
-    libgbm1 libpango-1.0-0 libasound2t64
-
-  шаг "Chrome $CHROME_VER"
-  # ИМЕННО ЭТА СБОРКА: эталоны `css_probe` учреждены на ней, а вычисленные
-  # стили машинно-зависимы. Другая сборка даст расхождение — и это не дефект
-  # проекта.
-  if [ -d "$HOME/chrome/linux-$CHROME_VER" ]; then
-    годно "уже есть"
+  # БОЕВОЙ МАШИНЕ CHROME НЕ НУЖЕН: он нужен только приёмке, а приёмка сносит
+  # philos_test и на рабочей машине не гоняется (philosphaira-hosting.md).
+  if [ "${PHILOS_NO_CHROME:-}" = 1 ]; then
+    шаг "Chrome пропущен (PHILOS_NO_CHROME=1)"
   else
-    ( cd "$HOME" && npx --yes @puppeteer/browsers install "chrome@$CHROME_VER" ) \
-      > /dev/null 2>&1 || беда "Chrome не поставился — приёмка приложения работать не будет"
-    годно "поставлен"
+    шаг "библиотеки для Chrome"
+    # Без них Chrome на безголовой Ubuntu не стартует и молчит невнятно.
+    # `libasound2t64` — имя именно для 24.04; старое `libasound2` не найдётся.
+    поставить libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 \
+      libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
+      libgbm1 libpango-1.0-0 libasound2t64
+
+    шаг "Chrome $CHROME_VER"
+    # ИМЕННО ЭТА СБОРКА: эталоны `css_probe` учреждены на ней, а вычисленные
+    # стили машинно-зависимы. Другая сборка даст расхождение — и это не дефект
+    # проекта.
+    if [ -d "$HOME/chrome/linux-$CHROME_VER" ]; then
+      годно "уже есть"
+    else
+      ( cd "$HOME" && npx --yes @puppeteer/browsers install "chrome@$CHROME_VER" ) \
+        > /dev/null 2>&1 || беда "Chrome не поставился — приёмка приложения работать не будет"
+      годно "поставлен"
+    fi
+
   fi
 
   шаг "служба PostgreSQL"
@@ -157,7 +165,16 @@ mkdir -p "$LOGD"
   [ -n "$SRC" ] || стоп "укажите архив или каталог: развернуть ~/philosphaira.zip"
 
   шаг "распаковка"
-  if [ -d "$SRC" ]; then
+  if [ -d "$SRC" ] && [ "$(realpath "$SRC")" = "$(realpath -m "$ROOT")" ]; then
+    # РАЗВЁРТЫВАНИЕ НА МЕСТЕ: каталог и есть корень — обычно клон git. Так
+    # обновление сводится к `git pull` (philosphaira-hosting.md, «Выкатка»).
+    # Прежде ветка каталога всегда копировала, и копия в саму себя
+    # останавливала скрипт.
+    for need in source tools server; do
+      [ -d "$ROOT/$need" ] || стоп "в «$ROOT» нет папки $need — это не дерево проекта"
+    done
+    годно "развёртывание на месте: $ROOT"
+  elif [ -d "$SRC" ]; then
     mkdir -p "$ROOT" && cp -r "$SRC"/. "$ROOT"/
     годно "скопировано из каталога"
   else
@@ -243,10 +260,12 @@ mkdir -p "$LOGD"
         && годно "$DB создана"
     fi
   done
+  # ПАРОЛЬ РОЛИ — СЛУЧАЙНЫЙ, и задаётся вместе с заведением окружения (ниже):
+  # прежде стоял 'philos', вшитый и в DATABASE_URL, — на машине с публичным
+  # адресом это пароль, известный каждому, кто читал этот скрипт.
   как_postgres psql -tAc \
     "SELECT 1 FROM pg_roles WHERE rolname='philos'" 2>/dev/null | grep -q 1 \
-    || как_postgres psql -c \
-      "CREATE ROLE philos LOGIN PASSWORD 'philos'" > /dev/null
+    || как_postgres psql -c "CREATE ROLE philos LOGIN" > /dev/null
   for DB in philos philos_test; do
     как_postgres psql -c \
       "ALTER DATABASE $DB OWNER TO philos" > /dev/null 2>&1 || true
@@ -258,8 +277,11 @@ mkdir -p "$LOGD"
     годно "$ENVF уже есть — не трогаю"
   else
     KEY=$(node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
+    DBPW=$(node -e "console.log(require('crypto').randomBytes(18).toString('base64url'))")
+    как_postgres psql -c "ALTER ROLE philos LOGIN PASSWORD '$DBPW'" > /dev/null \
+      || стоп "не удалось задать пароль роли philos"
     cat > "$ENVF" <<EOF
-DATABASE_URL=postgres://philos:philos@127.0.0.1:5432/philos
+DATABASE_URL=postgres://philos:$DBPW@127.0.0.1:5432/philos
 MFA_SECRET_KEY=$KEY
 PORT=8814
 # PUBLIC_URL=http://адрес-в-сети:8814   # корень ссылок в письмах
diff --git a/deploy/philosphaira-hosting.md b/deploy/philosphaira-hosting.md
index ea739a3..1213951 100644
--- a/deploy/philosphaira-hosting.md
+++ b/deploy/philosphaira-hosting.md
@@ -1,246 +1,357 @@
-# ΦilosΦaira: публичное размещение под своим именем
+# ΦilosΦaira: публичное размещение под своим именем — пошаговое руководство
 
-Этот документ **не повторяет** `philosphaira-ubuntu.md`. Тот отвечает на
-вопрос «как поднять узел», и отвечает полно: установка, две базы,
-переменные, миграции, семя, раскладка, администратор, службы, приёмка.
-Здесь — только то, что добавляется, когда узел перестаёт быть локальным:
-имя, TLS, ставень, файрвол, копии, выкатка.
+Это руководство, а не обзор: шаги идут в том порядке, в каком их надо
+делать, у каждого есть команды и **проверка**, без которой к следующему
+шагу не переходят. Порядок местами жёсткий, и где он жёсткий — сказано
+почему.
 
-**ГЛАВНОЕ РАСХОЖДЕНИЕ, ЕГО СТОИТ ПРОЧЕСТЬ ПЕРВЫМ.** `philosphaira-ubuntu.md`
-§3.3 и §11 предписывают не задавать `NODE_ENV=production` и `WS_ORIGINS`.
-Это верно ровно для HTTP в домашней сети. **За TLS обе переменные
-обязательны**, и без них служба ломается по-разному, но одинаково молча
-(см. §4). Оба документа правы каждый в своём случае — но случай надо знать
-до того, как открыть файл.
+Обозначения, которые надо заменить своим:
+
+| Обозначение | Что это | Пример |
+|---|---|---|
+| `ИМЯ` | ваш домен | `philosphaira.to` |
+| `АДРЕС` | публичный IPv4 машины | `203.0.113.10` |
+| `ЛОГИН` | ваш логин администратора в сервисе | `nyanko` |
+| `ПОЧТА` | ваш настоящий адрес | `вы@ваша.почта` |
+
+Команды помечены, где их набирать: **[у себя]** — на своей машине
+(Ubuntu-виртуалка, или Windows 10 с PowerShell — там есть `ssh`, `scp`,
+`ssh-keygen`), **[на сервере]** — в SSH-сеансе на арендованной машине.
+
+**Проверено 24 сентября 2026 прогоном:** развёртывание на месте в свежем клоне
+(шаги 3–4), случайный пароль роли базы под `scram-sha-256`, `git pull` в
+клоне после развёртывания. **Не проверено здесь** (нет домена, почты,
+systemd): шаги 7–11 — они написаны по коду проекта и документации Caddy,
+а проверка после каждого из них как раз для того, чтобы вы увидели
+расхождение на своей стороне сразу, а не при первом госте.
+
+---
+
+## 0. Что приготовить до начала
+
+1. **Домен.** Куплен, доступ к панели DNS есть. Для `.to` — любой регистратор,
+   торгующий зоной Тонги; занятость проверяется на `tonic.to/whois`.
+   Продление поставьте в календарь: у ccTLD нет привычных процедур ICANN,
+   просрочку исправлять труднее, чем в `.org`.
+2. **Поставщик почты с SMTP** (Postmark, Mailgun, Migadu, Fastmail — любой).
+   Регистрация в сервисе открытая, подтверждение адреса идёт письмом: без
+   почты люди заведутся, но не подтвердятся, и права у них будут срезаны.
+   Свой почтовый сервер на этой же машине не заводите — письма с нового
+   адреса в свежей ccTLD будут падать в спам, и чинить придётся чужие
+   чёрные списки. Понадобятся: адрес SMTP-сервера, порт, логин, пароль и
+   записи DNS, которые поставщик выдаст для домена.
+3. **Ключ SSH** [у себя]:
+   ```bash
+   ssh-keygen -t ed25519 -C "philosphaira"      # Enter на все вопросы
+   ```
+   Открытая половина — `~/.ssh/id_ed25519.pub` (в Windows —
+   `C:\Users\<вы>\.ssh\id_ed25519.pub`).
+4. **Хранилище для копий вне машины** — любое с SFTP. Ниже в примерах
+   Hetzner Storage Box; годится любой SFTP-адрес (§10).
+5. **Место для секретов вне сервера** (менеджер паролей): туда лягут
+   `MFA_SECRET_KEY`, пароль хранилища копий и пароль restic. Потеря
+   `MFA_SECRET_KEY` — это потеря всех заведённых вторых шагов, и никакой
+   дамп базы её не возместит.
 
 ---
 
-## 1. Что решить до того, как что-то покупать
+## 1. Машина
+
+Возьмите VPS: **Ubuntu 24.04 LTS, 2 vCPU, 4 ГБ памяти, от 40 ГБ диска**,
+с публичным IPv4 (например, Hetzner Cloud, линейка общих vCPU, дата-центр в
+Германии). Узел один, база маленькая, этого хватает с запасом. При создании
+вставьте свой открытый ключ SSH из §0.3 — тогда пароль root не понадобится
+вовсе.
 
-### 1.1. Письма
+Chrome 131 на боевую машину **не ставится**: он нужен только приёмке, а
+приёмка сносит испытательную базу и гоняется у вас, а не здесь.
 
-`scripts/notify-worker.mjs` берёт `nullSender`, а тот на любой отправке
-бросает «отправитель писем не настроен». Пока это так, **публичная
-регистрация не работает вовсе**: подтверждение адреса идёт письмом, а без
-подтверждённого адреса права срезаются (`NEEDS_VERIFIED_EMAIL`,
-`src/access/roles.js:73`).
+**Проверка** [у себя]:
+```bash
+ssh root@АДРЕС 'lsb_release -ds; nproc; free -h | head -2'
+# Ubuntu 24.04… · 2 · Mem: 3.7Gi…
+```
 
-**ВЫБРАНА ОТКРЫТАЯ РЕГИСТРАЦИЯ, И ОТПРАВИТЕЛЬ НАПИСАН.** Прежняя редакция
-этого раздела предлагала две ветки; ветка закрытого круга больше не нужна.
-Что появилось в дереве:
+---
+
+## 2. Человек, вход по ключу, обновления
+
+**Порядок жёсткий:** сперва убедиться, что входите новым пользователем по
+ключу, и **только потом** запрещать вход паролем и root. Наоборот — и вы
+заперты снаружи собственной машины.
+
+[на сервере, под root]:
+```bash
+adduser --disabled-password --gecos "" philos
+usermod -aG sudo philos
+passwd philos                     # пароль для sudo (не для входа)
+mkdir -p /home/philos/.ssh
+cp /root/.ssh/authorized_keys /home/philos/.ssh/
+chown -R philos:philos /home/philos/.ssh
+chmod 700 /home/philos/.ssh && chmod 600 /home/philos/.ssh/authorized_keys
+```
 
-- `server/src/notify/mail.js` — отправитель поверх SMTP (`nodemailer`,
-  версия закреплена точно, как и прочие шесть зависимостей);
-- работник берёт его **из окружения** (`SMTP_URL`, `MAIL_FROM`) и объявляет
-  вслух при запуске, задан он или нет;
-- отказы почты разделены на постоянные и временные (см. §9);
-- `/api/auth/register` получил предел по адресу (см. §4);
-- `probes/mail_probe.mjs`, 29 утверждений, в приёмке.
+**Проверка** [у себя, в НОВОМ окне, старое не закрывать]:
+```bash
+ssh philos@АДРЕС 'sudo -v && echo sudo-ok'
+```
 
-От вас остаются **записи в DNS** (§3.1) и выбор поставщика почты. Годится
-любой, у кого есть SMTP: Postmark, Mailgun, Migadu, Fastmail. Собственный
-почтовый сервер на той же машине заводить не стоит — доставляемость с
-нового адреса в свежей ccTLD будет плохой, и разбираться придётся не с
-кодом, а с чужими чёрными списками.
+Только если ответ `sudo-ok` — [на сервере]:
+```bash
+sudo sed -i -E 's/^#?PasswordAuthentication .*/PasswordAuthentication no/' /etc/ssh/sshd_config
+sudo sed -i -E 's/^#?PermitRootLogin .*/PermitRootLogin no/'              /etc/ssh/sshd_config
+sudo systemctl restart ssh
+sudo apt-get update && sudo apt-get install -y unattended-upgrades git
+sudo dpkg-reconfigure -f noninteractive unattended-upgrades
+```
 
-### 1.2. Что видит гость
+**Проверка** [у себя]: `ssh root@АДРЕС` должен получить отказ
+(`Permission denied (publickey)`), `ssh philos@АДРЕС` — войти.
 
-Гость получает **снимок** графа на миг открытия страницы и не получает
-сокета вовсе (`ws/manager.js` отвечает 401 без сеанса). Это решение, а не
-упущение, но для публичного случая следствие важнее, чем для локального:
-анонимный читатель — а их будет большинство — не увидит чужих правок до
-перезагрузки. Если это не годится, разводить личные извещения и правки
-графа по разным каналам придётся до открытия, а не после.
+Дальше всё — под `philos`.
 
 ---
 
-## 2. Машина
+## 3. Клон и установка
 
-**Hetzner CX22** (2 vCPU, 4 ГБ, 40 ГБ), Ubuntu 24.04 LTS, Nuremberg или
-Falkenstein. Узел здесь один, база маленькая, пик даёт не сервер.
+Проект кладётся **клоном git** прямо в `~/philosphaira`: так выкатка новой
+версии сводится к `git pull` (§11). `развернуть` умеет разворачивать на месте
+с 24 сентября 2026; прежде ветка каталога всегда копировала, и копия в саму
+себя останавливала скрипт.
 
-**Chrome 131 на боевой машине не нужен.** Он нужен приёмке
-(`philosphaira-ubuntu.md` §2.3, §8), а приёмка сносит `philos_test` и на
-рабочей машине не гоняется. Гонять её у себя, на боевую ставить только
-Node, PostgreSQL и зависимости. Это снимает и требование 6 ГБ диска, и
-список системных библиотек.
+[на сервере]:
+```bash
+cd ~
+git clone https://github.com/Ranatar/philosphaira.git
+PHILOS_NO_CHROME=1 bash ~/philosphaira/deploy/philos-ubuntu.sh установить
+```
 
-Установка и развёртывание — по `philosphaira-ubuntu.md`: короткий путь в §0
-(четыре команды скрипта), доводы и ручной путь в §2–4, из установки
-пропускается 2.3 (Chrome). Сеть настраивать не надо: у машины есть публичный
-адрес. Три отличия публичного случая — ниже, §4а.
+`PHILOS_NO_CHROME=1` пропускает Chrome и его библиотеки. Скрипт сам
+обновляет списки пакетов, ставит Node 22 (из NodeSource — не из репозиториев
+Ubuntu, там 18.19, и нативный `argon2` на нём лотерея) и PostgreSQL 16.
 
-Что добавляется к §2, потому что машина смотрит наружу:
+**Проверка**:
+```bash
+node -v                                   # v22.x
+sudo -u postgres psql -tAc 'select version()' | cut -c1-20   # PostgreSQL 16…
+```
+
+---
+
+## 4. Развёртывание: базы, окружение, семя, раскладка, администратор
 
+[на сервере]:
 ```bash
-# отдельный человек, вход по ключу, пароли выключены
-adduser --disabled-password philos
-usermod -aG sudo philos
-# ключ положить в /home/philos/.ssh/authorized_keys, затем:
-sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
-sudo sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
-sudo systemctl restart ssh
+cd ~/philosphaira
+BOOTSTRAP_ADMIN_LOGIN=ЛОГИН BOOTSTRAP_ADMIN_EMAIL=ПОЧТА \
+  bash deploy/philos-ubuntu.sh развернуть ~/philosphaira
+```
 
-sudo apt install -y unattended-upgrades
-sudo dpkg-reconfigure -plow unattended-upgrades
+Пароль администратора скрипт спросит с клавиатуры без эха — **так и
+вводите**: переданный переменной, он остаётся в истории оболочки и виден в
+`/proc`. Не короче 12 знаков.
+
+Что делает шаг: зависимости сервера, две базы (`philos` — рабочая,
+`philos_test` — для приёмки, её сносят), роль `philos` со **случайным**
+паролем (прежде стоял пароль `philos`, известный всякому, кто читал скрипт),
+`~/philos.env` с правами 600, 14 миграций, семя графа, первая раскладка,
+администратор с подтверждённой почтой (письмо ему не идёт — поэтому всё
+это делается до того, как заработает SMTP).
+
+**Проверка** — последние строки вывода:
+```
+▸ схема           ✓ всего 14, применено 14
+▸ семя графа      ✓ наборы внесены
+▸ первая раскладка ✓ раскладка посчитана и записана
+  ✓ ЛОГИН ДЛЯ ВХОДА: ЛОГИН
 ```
 
-Файрвол — в §6, после ставня: включать его раньше, чем поднят Caddy,
-значит запереть себя от проверки.
+**Сразу же** унесите ключ второго шага с машины [у себя]:
+```bash
+ssh philos@АДРЕС "grep '^MFA_SECRET_KEY=' ~/philos.env"
+```
+— и положите строку в менеджер паролей (§0.5).
 
 ---
 
-## 3. Имя и DNS
+## 5. Переменные публичного случая
 
-`.to` — зона Тонги, реестром правит Tonic Corporation, регистрация открыта
-иностранцам и частным лицам. По разным регистраторам цена идёт примерно от
-$21 до $200 в год — разброс десятикратный, смотреть стоит. Занятость
-проверяется на `tonic.to/whois`.
+`~/philos.env` после §4 годится для домашней сети. За TLS к нему добавляются
+шесть строк. **Кавычки обязательны**: файл читают и systemd, и bash
+(`set -a; . ~/philos.env`), и в bash строка `MAIL_FROM=ΦilosΦaira <…>` без
+кавычек — это перенаправление ввода из файла, на котором сломаются копии
+(§10) и всякая ручная команда. systemd одинарные кавычки снимает сам.
 
-Что держать в уме: это ccTLD без привычных процедур ICANN. Просрочка
-продления решается хуже, чем в `.org`, а споров о имени разбирать
-практически некому. Продление ставить в календарь, а не полагаться на
-письмо регистратора.
+[на сервере] — значения SMTP подставить от поставщика:
+```bash
+cat >> ~/philos.env <<'EOF'
+NODE_ENV=production
+TRUST_PROXY=1
+PUBLIC_URL=https://ИМЯ
+WS_ORIGINS=https://ИМЯ
+SMTP_URL='smtps://ЛОГИН_SMTP:ПАРОЛЬ_SMTP@smtp.поставщик.tld:465'
+MAIL_FROM='ΦilosΦaira <no-reply@ИМЯ>'
+EOF
+nano ~/philos.env        # заменить ИМЯ и SMTP-значения на настоящие
+```
 
-Записи:
+Если в пароле SMTP есть `@ : / ? # %` — их кодируют в URL (`@` → `%40`,
+`:` → `%3A`, `/` → `%2F`, `?` → `%3F`, `#` → `%23`, `%` → `%25`). Порт 465 —
+`smtps://`; если поставщик даёт 587 со STARTTLS — `smtp://…:587`.
 
-| Тип | Имя | Значение |
-|---|---|---|
-| A | `philosphaira.to` | адрес IPv4 машины |
-| AAAA | `philosphaira.to` | адрес IPv6, если есть |
-| A/AAAA | `www` | то же |
+Что каждая делает и что будет без неё:
+
+| Переменная | Без неё |
+|---|---|
+| `NODE_ENV=production` | cookie сеанса уходит без `Secure` и утечёт при первом обращении по HTTP |
+| `TRUST_PROXY=1` | при `production` узел **нарочно не поднимется**: за ставнем без доверия у всех один адрес `127.0.0.1`, и предел регистраций (5 в час с адреса) бьёт по всем разом |
+| `PUBLIC_URL` | ссылка подтверждения в письме уведёт человека на `http://127.0.0.1:8814` — его собственный компьютер |
+| `WS_ORIGINS` | Origin не проверяется вовсе; значение — адрес страницы **со схемой**: с `http://` страница откроется, а живые соединения молча отвергнутся |
+| `SMTP_URL`, `MAIL_FROM` | работник скажет первой строкой «ПОЧТА НЕ НАСТРОЕНА» и писем не шлёт; регистрация пройдёт, подтверждение — нет |
 
-TTL на время настройки — 300. Проверять до перехода к §5, потому что
-сертификат выдаётся по проверке имени, и пока запись не разошлась,
-выпустить его нельзя:
+Дополнительно, если понадобится: `PG_POOL_MAX` (умолчание 10 соединений),
+`PG_STATEMENT_TIMEOUT` (умолчание 15000 мс).
 
+**Проверка** — файл читается оболочкой без ошибок:
 ```bash
-dig +short philosphaira.to A
+( set -a; . ~/philos.env; set +a; echo "$MAIL_FROM | $PUBLIC_URL" )
 ```
 
-### 3.1. Записи для почты — обязательны при открытой регистрации
+---
 
-Письмо подтверждения от домена без этих записей уходит в спам, и человек,
-не получивший его, увидит сервис, где регистрация «не работает». Точные
-значения даёт поставщик почты; форма такова:
+## 6. Службы и второй шаг администратора
 
-| Тип | Имя | Значение |
-|---|---|---|
-| TXT | `philosphaira.to` | `v=spf1 include:<поставщик> -all` |
-| TXT | `<селектор>._domainkey` | открытый ключ DKIM от поставщика |
-| TXT | `_dmarc` | `v=DMARC1; p=quarantine; rua=mailto:вы@адрес` |
+[на сервере]:
+```bash
+cd ~/philosphaira
+bash deploy/philos-ubuntu.sh служба
+```
+
+Заводятся **две** службы: `philosphaira` (узел) и `philosphaira-worker`
+(работник уведомлений). Работник нужен всегда, а не только ради почты: без
+него чужая правка не доходит до открытых страниц вовсе, и молча.
+
+**Проверка**:
+```bash
+systemctl is-active philosphaira philosphaira-worker       # active, active
+journalctl -u philosphaira-worker -n 5 --no-pager           # «почта: smtp.… , от …»
+curl -s http://127.0.0.1:8814/index.html | grep -c 'name="philos-api"'   # 1
+```
+
+**Второй шаг администратора — сейчас, до DNS.** Пока его нет, у
+администратора срезаны опасные права (назначать роли, банить, рассматривать
+коммиты), а после §8 служба будет открыта миру. Панели ещё нет (имя не
+указывает на машину, а по HTTP вход при `production` не работает), поэтому —
+из командной строки:
 
-Проверять не глазами: отправьте письмо на свой ящик у крупной почтовой
-службы и посмотрите в заголовках `Authentication-Results` — там написано,
-сошлись ли SPF и DKIM. Заведённая, но не сошедшаяся запись выглядит точно
-так же, как отсутствующая.
+```bash
+cd ~/philosphaira/server
+set -a; . ~/philos.env; set +a
+node scripts/enroll-mfa.mjs ЛОГИН
+```
+
+Скрипт покажет секрет строкой — введите её в приложение-аутентификатор
+(Aegis, Authy, Google Authenticator) руками — и включит второй шаг **только
+после** того, как вы введёте шестизначный код. Коды восстановления, если он их
+выдаст, — в менеджер паролей.
 
 ---
 
-## 4. Переменные окружения: чем публичный случай отличается
+## 7. Файрвол
 
-К `~/philos.env` из `philosphaira-ubuntu.md` §3.3 добавляются три строки, и
-две из них там прямо запрещены — потому что запрет относился к HTTP:
+Узел слушает **все** интерфейсы (`httpServer.listen(port)` в
+`server/src/http/server.js` без адреса), и `http://АДРЕС:8814` иначе окажется
+открыт в обход ставня. Вход там сломан (`Secure`-cookie по HTTP), чтение
+работает — дыра, которая выглядит как поломка.
 
-```ini
-NODE_ENV=production
-WS_ORIGINS=https://philosphaira.to
-PUBLIC_URL=https://philosphaira.to
-TRUST_PROXY=1
-SMTP_URL=smtps://пользователь:пароль@smtp.поставщик.tld:465
-MAIL_FROM=ΦilosΦaira <no-reply@philosphaira.to>
-```
-
-Доводы, а не просто перечень:
-
-- `NODE_ENV=production` включает `безопасныеCookie`. По HTTP это ломает
-  вход (браузер не сохранит cookie с `Secure`, и человек окажется гостем
-  без внятной ошибки) — потому в локальной инструкции и стоит запрет. За
-  TLS всё наоборот: **без этой переменной cookie сеанса уходит без
-  `Secure`**, то есть утекает при первом же обращении по HTTP.
-- `WS_ORIGINS` не задан — значит Origin не проверяется вовсе. В домашней
-  сети это допустимо, в открытом мире — нет. Значение должно совпадать с
-  адресом страницы **вместе со схемой**; `http://` вместо `https://`
-  оставит страницу рабочей, а живые соединения молча отвергнутыми.
-- `PUBLIC_URL` подставляется в ссылки писем (`src/auth/service.js:38`).
-  Умолчание — `http://127.0.0.1:8814`, и без переменной подтверждение
-  адреса уедет человеку ссылкой на его собственный компьютер.
-- `TRUST_PROXY` — сколько ставней перед узлом. **Без него узел при
-  `NODE_ENV=production` не поднимется, и это нарочно**: за ставнем без
-  доверия ему `req.ip` равен `127.0.0.1` у всех, адреса в `user_sessions` и
-  `audit_log` перестают отвечать на вопрос «откуда заходили», а предел
-  регистраций (ниже) бьёт по всем разом. Доверять всегда тоже нельзя: без
-  ставня любой присланный `X-Forwarded-For` станет «адресом человека».
-- `SMTP_URL` и `MAIL_FROM` — почта. Не заданы: работник скажет об этом
-  первой строкой и не будет слать никуда; регистрация при этом пройдёт, а
-  подтверждение адреса — нет.
-
-**Предел регистраций.** С одного адреса заводится пять записей в час, шестая
-получает `429`. Считается всякая попытка, а не только неудачная: вредна как
-раз удачная, потому что каждая заводит запись и письмо, а письма на мёртвые
-адреса портят доброе имя отправителя. Счётчик живёт **в памяти процесса** —
-при нескольких узлах каждый будет считать своё (то же ограничение, что у
-защиты входа; общее хранилище — отдельная работа).
+**Порядок:** `OpenSSH` разрешить **до** `enable`, иначе сеанс оборвётся и
+войти будет нечем.
+
+[на сервере]:
+```bash
+sudo ufw default deny incoming
+sudo ufw default allow outgoing
+sudo ufw allow OpenSSH
+sudo ufw allow 80,443/tcp
+sudo ufw --force enable
+sudo ufw status verbose
+```
+
+**Проверка** [у себя] — снаружи порт узла должен молчать:
+```bash
+curl --max-time 5 -sI http://АДРЕС:8814/index.html; echo "код $?"   # код 28 (время вышло)
+```
 
 ---
 
-## 4а. Развёртывание: три отличия от локального случая
+## 8. DNS
 
-Порядок команд и заведение администратора расписаны в
-`philosphaira-ubuntu.md` §0 и §4 и здесь не повторяются: два описания одного
-действия расходятся на первой же правке, и тогда непонятно, которое верно.
-Отличается публичный случай ровно тремя вещами.
+В панели регистратора:
 
-1. **Второй шаг завести ДО того, как имя начнёт указывать на машину.**
-   В домашней сети его можно отложить, здесь — нет: до второго шага у
-   администратора срезаны опасные права, а служба между тем уже открыта
-   всему миру. Порядок: `развернуть` → второй шаг (§4.4 той инструкции) →
-   и только потом A-запись из §3.
-2. **Пароль администратора не передавать переменной в командной строке.**
-   Она остаётся в истории оболочки и видна в `/proc` любому на машине.
-   `развернуть` спрашивает пароль с клавиатуры без эха — этим и
-   пользоваться; логин и почта задаются переменными, пароль нет.
-3. **Почта администратора считается подтверждённой сразу, письмо ему не
-   идёт.** Значит поднять службу и завести администратора можно ДО того, как
-   заработает SMTP, и не ждать поставщика почты.
+| Тип | Имя | Значение | TTL |
+|---|---|---|---|
+| A | `@` (сам `ИМЯ`) | `АДРЕС` | 300 |
+| A | `www` | `АДРЕС` | 300 |
+| AAAA | `@` и `www` | IPv6 машины — **только если** он у неё есть и `ufw` его пропускает | 300 |
 
-## 5. Ставень с TLS
+**Проверка** [у себя] — до перехода к §9, потому что сертификат выпускается
+по проверке имени:
+```bash
+dig +short ИМЯ A          # АДРЕС
+dig +short www.ИМЯ A      # АДРЕС
+```
+Пусто — подождать и повторить. TTL после запуска можно поднять до 3600.
+
+---
 
-Страницу отдаёт сам узел (`express.static` на `../app`,
-`src/http/app.js:177`), поэтому ставню остаётся только проксировать —
-отдельная раздача статики не нужна и вредна: без узла в разметку не попадёт
-метка `philos-api`, по которой клиент узнаёт, что сервер есть.
+## 9. Caddy: TLS и ставень
 
-**Caddy** — потому что сертификат он берёт и продлевает сам, а
-`Upgrade` для сокета пробрасывает без настройки:
+Caddy берёт и продлевает сертификат сам и пробрасывает WebSocket без
+настройки. Страницу отдаёт сам узел (в ней метка `philos-api`, по которой
+клиент узнаёт, что сервер есть), поэтому Caddy **только проксирует** —
+раздавать `app/` мимо узла нельзя.
 
+[на сервере]:
 ```bash
-sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
+sudo apt-get install -y debian-keyring debian-archive-keyring apt-transport-https curl
 curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' \
   | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
 curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' \
   | sudo tee /etc/apt/sources.list.d/caddy-stable.list
-sudo apt update && sudo apt install -y caddy
-```
+sudo apt-get update && sudo apt-get install -y caddy
 
-`/etc/caddy/Caddyfile` целиком:
+sudo tee /etc/caddy/Caddyfile > /dev/null <<'EOF'
+www.ИМЯ {
+	redir https://ИМЯ{uri} permanent
+}
 
-```
-philosphaira.to, www.philosphaira.to {
+ИМЯ {
 	encode zstd gzip
 	reverse_proxy 127.0.0.1:8814
 }
+EOF
+sudo sed -i 's/ИМЯ/philosphaira.to/g' /etc/caddy/Caddyfile    # ← своё имя
+sudo systemctl reload caddy
 ```
 
+`www` перенаправляется на голое имя, а не обслуживается отдельно: страница
+всегда открывается с одного адреса, и `WS_ORIGINS` из §5 достаточно одного.
+
+**Проверка** [у себя]:
 ```bash
-sudo systemctl reload caddy
-curl -I https://philosphaira.to/index.html
+curl -sI https://ИМЯ/index.html | head -1                   # HTTP/2 200
+curl -s  https://ИМЯ/index.html | grep -c 'name="philos-api"'   # 1
+curl -sI http://ИМЯ/  | grep -i '^location'                 # https://ИМЯ/
+curl -sI https://www.ИМЯ/ | grep -i '^location'             # https://ИМЯ/
 ```
 
-Если хочется nginx — годится, но тогда `Upgrade` и `Connection`
-прописываются руками, иначе сокет не поднимется, а страница будет
-работать: та же тихая поломка, что и с `WS_ORIGINS`.
+Затем в браузере: `https://ИМЯ` → вход `ЛОГИН` → код второго шага → панель
+открывается, права администратора на месте. **Живое обновление** проверяется
+вторым браузером (или окном инкогнито с другим пользователем): правка в одном
+окне должна появиться во втором без перезагрузки.
 
+Если вместо Caddy nginx — `Upgrade` и `Connection` прописываются руками,
+иначе страница работает, а сокет нет:
 ```nginx
 location / {
     proxy_pass http://127.0.0.1:8814;
@@ -255,162 +366,228 @@ location / {
 
 ---
 
-## 6. Файрвол — не «на всякий случай»
+## 10. Почта: записи DNS и проверка доставки
+
+Точные значения выдаёт поставщик; форма такова:
+
+| Тип | Имя | Значение |
+|---|---|---|
+| TXT | `@` | `v=spf1 include:<от поставщика> -all` |
+| TXT или CNAME | `<селектор>._domainkey` | ключ DKIM от поставщика |
+| TXT | `_dmarc` | `v=DMARC1; p=quarantine; rua=mailto:ПОЧТА` |
+
+В панели поставщика дождитесь, пока домен отмечен подтверждённым.
 
-`serve.mjs` передаёт в `listen` только порт (`src/http/server.js:22`),
-хоста задать нечем, значит узел слушает **на всех интерфейсах**. На
-виртуалке за NAT это незаметно; на машине с публичным адресом
-`http://IP:8814` окажется открыт в обход ставня — и там `NODE_ENV=production`
-сделает своё дело: вход по этому адресу ломается, а чтение работает. То
-есть дыра ещё и выглядит как поломка, а не как дыра.
+**Проверка доставки** — не глазами. В браузере без входа зарегистрируйте
+пробного пользователя на свой ящик у крупной почтовой службы, откройте
+пришедшее письмо, «Показать оригинал», найдите `Authentication-Results`:
+там должно стоять `spf=pass` и `dkim=pass`. Заведённая, но не сошедшаяся
+запись выглядит в панели DNS точно так же, как верная. Ссылка в письме
+обязана начинаться с `https://ИМЯ/` (иначе — `PUBLIC_URL`, §5). Пробного
+пользователя затем удалить из панели.
 
+Если письма нет вовсе:
 ```bash
-sudo ufw default deny incoming
-sudo ufw default allow outgoing
-sudo ufw allow OpenSSH
-sudo ufw allow 80,443/tcp
-sudo ufw enable
-sudo ufw status                     # 8814 быть не должно
-curl --max-time 5 http://<адрес>:8814/index.html   # должно молчать
+journalctl -u philosphaira-worker -n 30 --no-pager
+set -a; . ~/philos.env; set +a
+psql "$DATABASE_URL" -c "SELECT id, last_error FROM outbox WHERE last_error IS NOT NULL ORDER BY id DESC LIMIT 5"
 ```
 
-Последняя строка — проверка, а не украшение: правило, которое не проверено
-снаружи, не правило.
-
 ---
 
-## 7. Адреса за ставнем — сделано
-
-Прежняя редакция называла это незакрытым: `trust proxy` не был выставлен
-нигде, и за ставнем в `user_sessions` и `audit_log` лёг бы `127.0.0.1` у
-всех и всегда. Теперь настройка проведена доводом от `serve.mjs` до
-`createApp`, умолчание — **не доверять**, а выбор требуется от человека
-(§4). Проверено пробой на двух узлах, отличающихся одним доводом: без
-доверия присланный `X-Forwarded-For` в базу не попадает, с доверием
-попадает.
+## 11. Копии — до открытия, а не после
 
----
+`app/data` — только **семя**; всё, что внесут люди, живёт в базе. Копия
+считается копией, когда (1) она вне машины и (2) из неё проверенно
+поднимается граф.
 
-## 8. Копии — до открытия, а не после
+### 11.1. Ежесуточный дамп на машине
 
-Пока пользователь один, потеря базы стоит времени. Как только правки
-начнут вносить другие, она станет невозместимой: `app/data` — это **семя**,
-рабочее состояние живёт в базе.
+[на сервере]:
+```bash
+mkdir -p ~/backups
+crontab -e
+```
+Строка (одна, целиком):
+```cron
+0 3 * * * set -a; . /home/philos/philos.env; set +a; pg_dump -Fc "$DATABASE_URL" > /home/philos/backups/philos-$(date +\%F).dump && find /home/philos/backups -name 'philos-*.dump' -mtime +14 -delete
+```
 
+**Проверка** — выполнить то же руками и увидеть файл:
 ```bash
-sudo -u philos crontab -e
+set -a; . ~/philos.env; set +a
+pg_dump -Fc "$DATABASE_URL" > ~/backups/philos-$(date +%F).dump && ls -lh ~/backups
 ```
 
+### 11.2. Вывоз наружу (restic по SFTP)
+
+[на сервере]:
+```bash
+sudo apt-get install -y restic
+ssh-keygen -t ed25519 -N "" -f ~/.ssh/backup_key
+cat ~/.ssh/backup_key.pub       # добавить в хранилище как ключ для SFTP
+```
+Для Hetzner Storage Box: включить SSH в панели, добавить ключ; адрес вида
+`uNNNNN@uNNNNN.your-storagebox.de`, порт 23. Затем:
+```bash
+cat >> ~/.ssh/config <<'EOF'
+Host backupbox
+  HostName uNNNNN.your-storagebox.de
+  User uNNNNN
+  Port 23
+  IdentityFile ~/.ssh/backup_key
+EOF
+head -c 24 /dev/urandom | base64 > ~/.restic-pass && chmod 600 ~/.restic-pass
+cat ~/.restic-pass              # → в менеджер паролей (§0.5): без него копия не откроется
+export RESTIC_REPOSITORY=sftp:backupbox:philosphaira RESTIC_PASSWORD_FILE=~/.restic-pass
+restic init
+restic backup ~/backups ~/philos.env
+```
+В cron — второй строкой, через час после дампа:
 ```cron
-0 3 * * * set -a; . /home/philos/philos.env; set +a; \
-  pg_dump -Fc "$DATABASE_URL" > /home/philos/копии/philos-$(date +\%F).dump && \
-  find /home/philos/копии -name 'philos-*.dump' -mtime +14 -delete
+0 4 * * * RESTIC_REPOSITORY=sftp:backupbox:philosphaira RESTIC_PASSWORD_FILE=/home/philos/.restic-pass restic backup -q /home/philos/backups /home/philos/philos.env && RESTIC_REPOSITORY=sftp:backupbox:philosphaira RESTIC_PASSWORD_FILE=/home/philos/.restic-pass restic forget -q --keep-daily 14 --keep-monthly 6 --prune
 ```
 
-Три условия, без которых это не копия:
+**Проверка**: `restic snapshots` показывает снимок с сегодняшней датой.
 
-1. **Вывоз наружу.** Дамп на той же машине не переживёт её потери:
-   `restic` или `rclone` в чужое хранилище.
-2. **Проверенное восстановление.** Раз в месяц поднять дамп в пустую базу
-   и посмотреть, что граф на месте. Непроверенная копия — не копия, а
-   намерение.
-3. **`MFA_SECRET_KEY` отдельно и вне машины.** Потеря ключа = потеря всех
-   заведённых вторых шагов, и никакой дамп базы этого не вернёт.
+### 11.3. Проверка восстановления — раз в месяц
 
-Рукотворное (`server/`, `source/`, `decisions/`, `tools/`) бережётся
-репозиторием, но зеркало на другой площадке лишним не будет.
+[на сервере]:
+```bash
+set -a; . ~/philos.env; set +a
+sudo -u postgres createdb philos_restore --owner=philos --locale=C.UTF-8 --template=template0
+F=$(ls -t ~/backups/philos-*.dump | head -1)
+pg_restore --no-owner -d "${DATABASE_URL%/philos}/philos_restore" "$F"
+psql "${DATABASE_URL%/philos}/philos_restore" -tAc "SELECT count(*) FROM graph_entities"   # не ноль
+sudo -u postgres dropdb philos_restore
+```
+
+Восстановление по-настоящему (после потери базы):
+```bash
+sudo systemctl stop philosphaira philosphaira-worker
+pg_restore --clean --if-exists --no-owner -d "$DATABASE_URL" ~/backups/philos-ГГГГ-ММ-ДД.dump
+sudo systemctl start philosphaira philosphaira-worker
+```
 
 ---
 
-## 9. Наблюдение
+## 12. Наблюдение
 
-Признака отставшего работника уведомлений в продукте нет (`doc/plan-next.md`,
-этап F), а без работника **чужая правка не доходит до открытых страниц
-вовсе** и молча. Единственный способ узнать — спросить базу:
+Показателя отставшего работника в продукте нет (`doc/plan-next.md`, этап F),
+а без работника чужие правки молча не доходят до страниц. Письмо о беде
+шлётся тем же SMTP, что и письма сервиса — `curl` умеет SMTP сам, отдельный
+почтовый агент не нужен.
 
+[на сервере]:
+```bash
+mkdir -p ~/bin
+cat > ~/bin/philos-watch.sh <<'EOF'
+#!/usr/bin/env bash
+set -a; . /home/philos/philos.env; set +a
+TO='ПОЧТА'
+say() { printf 'From: %s\nTo: %s\nSubject: philosphaira: %s\n\n%s\n' "$MAIL_FROM" "$TO" "$1" "$2" \
+  | curl -s --ssl-reqd --url "$SMTP_URL" --mail-from "no-reply@${PUBLIC_URL#https://}" --mail-rcpt "$TO" -T -; }
+for s in philosphaira philosphaira-worker caddy; do
+  systemctl is-active -q "$s" || say "служба $s стоит" "$(systemctl status "$s" --no-pager | head -20)"
+done
+n=$(psql "$DATABASE_URL" -tAc "SELECT count(*) FROM outbox WHERE delivered_at IS NULL")
+[ "$n" -gt 200 ] && say "исходящие копятся: $n" "работник стоит или отстал"
+d=$(df --output=pcent / | tail -1 | tr -dc 0-9)
+[ "$d" -gt 85 ] && say "диск заполнен на $d%" "$(du -sh /home/philos/backups)"
+exit 0
+EOF
+sed -i "s/'ПОЧТА'/'вы@ваша.почта'/" ~/bin/philos-watch.sh     # ← свой адрес
+chmod 700 ~/bin/philos-watch.sh
+crontab -e
+```
 ```cron
-*/15 * * * * set -a; . /home/philos/philos.env; set +a; \
-  n=$(psql "$DATABASE_URL" -tAc "SELECT count(*) FROM outbox WHERE delivered_at IS NULL"); \
-  [ "$n" -gt 200 ] && echo "исходящие копятся: $n" | mail -s philosphaira вы@адрес
+*/15 * * * * /home/philos/bin/philos-watch.sh
 ```
 
-Второй запрос — про почту, и он нужен именно при открытой регистрации.
-Постоянный отказ (`5xx`, негодный конверт) и исчерпание попыток закрывают
-строку исходящих, чтобы мёртвые письма не копились в очереди вечно и не
-отравляли счёт выше. Но закрытая безнадёжная строка сохраняет ошибку, и по
-ней видно, сколько адресов не принимает писем никогда:
+**Проверка** — письмо должно прийти:
+```bash
+bash -c 'set -a; . ~/philos.env; set +a; printf "Subject: philosphaira: проверка\n\nok\n" | curl -s --ssl-reqd --url "$SMTP_URL" --mail-from "no-reply@${PUBLIC_URL#https://}" --mail-rcpt "вы@ваша.почта" -T - && echo отправлено'
+```
 
+Раз в неделю глазами — закрытые безнадёжные письма (адреса, не принимающие
+почту). Быстрый рост — это либо боты с выдуманными адресами, либо ваши
+письма перестали принимать вовсе (§10):
 ```sql
 SELECT count(*) FROM outbox WHERE delivered_at IS NOT NULL AND last_error IS NOT NULL;
 ```
 
-Быстрый рост здесь — это либо наплыв ботов с выдуманными адресами (смотрите
-предел регистраций), либо ваши письма перестали принимать вовсе (смотрите
-SPF/DKIM/DMARC из §3.1).
-
-Плюс обычное: `systemctl status philosphaira philosphaira-worker`, место на
-диске, срок сертификата (Caddy продлевает сам, но молчаливый отказ
-продления — известный род беды).
-
 ---
 
-## 10. Выкатка новой версии
-
-`app/` — **порождаемое**, и собирать его на боевой машине не надо: там нет
-ни Chrome, ни приёмки, а сборка стирает дерево первым делом. Порядок:
+## 13. Выкатка новой версии
 
-1. У себя: правка исходника по `readme.md` §5, `remap.mjs собрать`, **полная
-   приёмка**.
-2. Коммит вместе с порождённым `app/`.
-3. На машине:
+`app/` — порождаемое; на боевой машине его не собирают: там нет Chrome и
+приёмки, а сборка стирает дерево первым делом.
 
+1. [у себя] Правка исходника по `readme.md` §5, `remap.mjs собрать`,
+   **полная приёмка** (`bash tools/accept.sh`) — зелёная.
+2. [у себя] Коммит **вместе с порождённым `app/`**, `git push`.
+3. [на сервере]:
 ```bash
-cd ~/philosphaira && git pull
+cd ~/philosphaira && git pull --ff-only
 cd server && npm ci
-npm run migrate                      # ДО перезапуска, а не после
+set -a; . ~/philos.env; set +a
+npm run migrate                       # ДО перезапуска, а не после
 sudo systemctl restart philosphaira philosphaira-worker
 ```
 
-Миграции идут до перезапуска: узел, поднятый на старой схеме, успеет
-ответить на запросы и записать в базу то, чего новая схема не ждёт.
+Миграции — до перезапуска: узел, поднятый на старой схеме, успеет принять
+запросы и записать то, чего новая схема не ждёт. Раскладку при выкатке
+**не пересчитывать** (`relayout` не звать): она общая для всех и
+доращивается сама при утверждённых коммитах; полная перекладка сдвигает
+узлы у всех разом.
+
+**Проверка**:
+```bash
+systemctl is-active philosphaira philosphaira-worker
+curl -s https://ИМЯ/index.html | grep -c 'name="philos-api"'     # 1
+```
+И обновить открытую страницу в браузере: `app/` отдаётся с диска.
+
+`git pull --ff-only` откажет, если на машине правили отслеживаемые файлы:
+так и задумано — на боевой машине файлы проекта руками не правят.
 
 ---
 
-## 11. Грабли публичного случая
+## 14. Если что-то пошло не так
 
-| Признак | Причина | Лечение |
+| Признак | Причина | Что делать |
 |---|---|---|
-| Вошли — и сразу гость | `NODE_ENV=production` **отсутствует**, а вы за TLS? нет: наоборот, он есть, а обращение идёт по HTTP | ходить по `https://`, HTTP перенаправлять |
-| Правило из `philosphaira-ubuntu.md` §11 не работает | та таблица писана для HTTP в домашней сети | здесь §4, там §11; случаи разные |
-| Страница открылась, живого обновления нет | `WS_ORIGINS` со схемой `http://` вместо `https://` | привести к адресу страницы буквально |
-| Ссылка в письме ведёт на `127.0.0.1` | не задан `PUBLIC_URL` | §4 |
-| Сервис доступен по `http://IP:8814` | узел слушает на всех интерфейсах, хоста задать нечем | закрыть файрволом (§6) |
-| В журнале у всех адрес `127.0.0.1` | нет `trust proxy` | §7, пока считать поле пустым |
-| Регистрация проходит, письмо не приходит | не заданы `SMTP_URL`/`MAIL_FROM` — работник говорит об этом первой строкой | §4 |
-| Письма уходят, но попадают в спам | нет SPF/DKIM/DMARC у домена | §3.1 |
-| Узел не поднимается, ругается на `TRUST_PROXY` | `NODE_ENV=production` без объявленного числа ставней | §4; осознанное «ставня нет» — `TRUST_PROXY=0` |
-| Регистрация отвечает `429` всем подряд | предел считает по `req.ip`, а он у всех один: не задан `TRUST_PROXY` | §4 |
-| В `outbox` растёт число закрытых строк с `last_error` | адреса, не принимающие писем | §9 |
-| Сертификат не выпустился | A-запись ещё не разошлась | `dig +short` и подождать |
+| Узел не поднимается, в журнале «задайте TRUST_PROXY» | `NODE_ENV=production` без числа ставней | `TRUST_PROXY=1` в `~/philos.env` (§5) |
+| Вошли — и сразу гость | обращение по `http://`, а cookie с `Secure` | ходить по `https://ИМЯ`; Caddy перенаправляет HTTP сам |
+| Страница есть, живого обновления нет | `WS_ORIGINS` не совпадает с адресом буквально (схема, `www`) | `https://ИМЯ`, открывать без `www` (Caddy перенаправит) |
+| Живого обновления нет, `WS_ORIGINS` верен | стоит работник | `systemctl status philosphaira-worker` |
+| `set -a; . ~/philos.env` ругается «No such file or directory» | значение с `<…>` без кавычек | одинарные кавычки (§5) |
+| Регистрация проходит, письма нет | нет `SMTP_URL`/`MAIL_FROM` или неверный пароль SMTP | первая строка журнала работника; `outbox.last_error` (§10) |
+| Письмо в спаме | SPF/DKIM не сошлись | `Authentication-Results` (§10) |
+| Ссылка в письме ведёт на `127.0.0.1` | нет `PUBLIC_URL` | §5, затем `systemctl restart philosphaira` |
+| Регистрация отвечает `429` всем | предел считает по одному адресу `127.0.0.1` — нет `TRUST_PROXY` | §5 |
+| Сертификат не выпустился | A-запись ещё не разошлась, или 80/443 закрыты | `dig +short ИМЯ`; `sudo ufw status`; `journalctl -u caddy -n 50` |
+| Снаружи открыт `http://АДРЕС:8814` | файрвол не включён | §7 |
+| Администратор не может назначить роль | нет второго шага | §6, `enroll-mfa.mjs` |
+| `git pull` отказывает | на машине правили файлы проекта | `git status`; вернуть: `git checkout -- <файл>` |
+| Рабочая база опустела | приёмку запустили на `DATABASE_URL` рабочей базы | восстановление §11.3; приёмку на сервере не гонять |
 
 ---
 
-## 12. Что в дереве осталось незакрытым
-
-- **Счётчик регистраций живёт в памяти процесса.** При одном узле это
-  верно; при нескольких каждый будет считать своё. То же ограничение у
-  защиты входа, и лечится оно тем же общим хранилищем.
-- **Показателя отставшего работника в самом продукте по-прежнему нет** —
-  только запросы из §9 (`doc/plan-next.md`, этап F).
-- **Кириллические имена в `server/src` и в исходнике.** `tools/checks/ops_probe.mjs`
-  красный на двух утверждениях: 32 имени в `server/src` (почти все — в
-  `graph/layout.js` и `graph/relayout-service.js`) и шесть в исходнике. Это
-  расхождение **уже было в репозитории** и к размещению отношения не имеет,
-  но приёмка приложения из-за него красная, а красный прибор, к которому
-  привыкли, перестаёт что-либо стеречь.
-
-## 13. Осталось выяснить не по технике
-
-Публичный сервис с учётными записями, поднятый из Германии, тянет за собой
-Impressum и описание обработки данных. Это не программистский вопрос, но
-выяснять его надо до открытия, а не после первого письма.
+## 15. Чего в продукте пока нет
+
+- **Показателя отставшего работника** — только проверка из §12
+  (`doc/plan-next.md`, этап F).
+- **Общего хранилища для пределов**: счётчики регистраций и попыток входа
+  живут в памяти процесса. При одном узле это верно; второй узел будет
+  считать своё.
+- **Живого обновления у гостя**: гость получает снимок графа на миг открытия
+  страницы и сокета не получает (`ws/manager.js` отвечает 401 без сеанса).
+  Анонимный читатель не увидит чужих правок до перезагрузки. Если это не
+  годится для публичного сервиса — решать до открытия.
+
+## 16. Не по технике — до открытия
+
+Публичный сервис с учётными записями на машине в Германии требует Impressum
+и описания обработки данных (почта, адреса в журналах сеансов). Это вопрос
+не к коду, но выяснять его — до первого гостя, а не после первого письма.
diff --git a/deploy/philosphaira-ubuntu.md b/deploy/philosphaira-ubuntu.md
index 00d5ab7..8af6a6b 100644
--- a/deploy/philosphaira-ubuntu.md
+++ b/deploy/philosphaira-ubuntu.md
@@ -48,6 +48,12 @@ bash philos-ubuntu.sh состояние            # проверить, что
 
 Остальные действия: `стоп`, `служба`, `приёмка`.
 
+**Два лада, которые нужны публичному размещению** (`philosphaira-hosting.md`):
+`PHILOS_NO_CHROME=1 bash philos-ubuntu.sh установить` — без Chrome и его
+библиотек (боевой машине они не нужны); `развернуть <каталог>`, где каталог и
+есть корень проекта (обычно клон git), — развёртывание на месте, после
+которого новая версия приходит через `git pull`.
+
 Скрипт проверен прогоном на Ubuntu 24.04: развёртывание **из каталога**,
 14 миграций, семя, раскладка, администратор, подъём и остановка обоих
 процессов. Путь **через архив** тогда прогнан не был — и в нём жил изъян,
@@ -340,7 +346,7 @@ node scripts/enroll-mfa.mjs nyanko        # ПРИМЕР; без довода 
 npm start
 ```
 
-Ответом будет `ΦilosΦaira поднята: http://127.0.0.1:8814/`. Адрес в сообщении — только текст: в `server.js:22` стоит `сервер.listen(порт)` без адреса, узел слушает все интерфейсы. Ничего править не надо.
+Ответом будет `ΦilosΦaira поднята: http://127.0.0.1:8814/`. Адрес в сообщении — только текст: в `server/src/http/server.js` стоит `httpServer.listen(port)` без адреса, узел слушает все интерфейсы. Ничего править не надо.
 
 Файрвол в Ubuntu 24 по умолчанию выключен; если включали — `sudo ufw allow 8814/tcp`.
 
